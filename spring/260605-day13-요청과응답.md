## **ObjectMapper와 리플렉션**



object mapper는 Jackson 라이브러리의 핵심 클래스로, Java 객체 ↔ JSON 간 변환을 담당합니다.

Spring이 @RestController에서 자동으로 JSON 변환을 해주긴 하지만, 

직접 JSON을 다뤄야 할 때 ObjectMapper를 명시적으로 주입받아 씁니다.



2차원 객체

┌────┐

│name│

│age │   →   {"name":"Kim","age":20}  →  네트워크 전송

└────┘              일렬로 직렬화



**ObjectMapper 직렬화 순서**



1\. Reflection으로 클래스 구조 분석

2\. Getter 메서드 (또는 public 필드) 탐색

3\. 메서드명에서 키 이름 추출 (getId → "id")

4\. 값을 JSON 타입에 맞게 변환

&nbsp;       String  → "..."

&nbsp;       Number  → 숫자

&nbsp;       Boolean → true/false

&nbsp;       Object  → { } 재귀적으로 반복

&nbsp;       List    → \[ ]

5\. JSON 문자열 완성{"id": 1, "name": "Kim"}



기본적으로 Getter 메서드를 기준으로 읽습니다.



@NoArgsConstructor  →  역직렬화 (JSON → 객체) 에서만 필요 /  객체 생성용  빈 객체를 먼저 만들기 때문입니다.

@Getter             →  직렬화   (객체 → JSON) 에서만 필요 / 값 읽기용



**자주 쓰는 Jackson 어노테이션**



@JsonProperty("user\_id")   // JSON 키 이름 변경

@JsonIgnore                // 직렬화에서 제외

@JsonFormat(pattern = "yyyy-MM-dd")  // 날짜 형식 지정



## **Optional**



**JPA Repository 기본 제공 메서드**

**Spring Data JPA에서 findById()는 이미 Optional로 반환하도록 내부적으로 선언되어 있습니다.**

**java**

**Optional<User> findById(Long id);  // 있을 수도, 없을 수도 있음**



**// 1. 없으면 예외 던지기 — 가장 많이 씀**

User user = userRepository.findById(id)

&nbsp;   .orElseThrow(() -> new RuntimeException("유저 없음"));





// 2. 없으면 기본값 반환

User user = userRepository.findById(id)

&nbsp;   .orElse(new User("기본유저"));





// 3. 있을 때만 실행

userRepository.findById(id)

&nbsp;   .ifPresent(user -> System.out.println(user.getName()));





// 4. 값 변환해서 꺼내기

String name = userRepository.findById(id)

&nbsp;   .map(User::getName)

&nbsp;   .orElse("이름없음");



**메서드 체이닝 중간에 null이 있을 때**



// 위험한 코드 💥

String city = user.getAddress().getCity(); // address가 null이면 NPE



// Optional로 안전하게

String city = Optional.ofNullable(user)

&nbsp;   .map(User::getAddress)

&nbsp;   .map(Address::getCity)

&nbsp;   .orElse("도시 없음");



## **blank  vs empty**

public class UserRequest {



&nbsp;   @NotNull   // null만 걸러냄, "" 통과 ❌

&nbsp;   private String name;



&nbsp;   @NotEmpty  // null + "" 둘 다 걸러냄 ✅

&nbsp;   private String name;



&nbsp;   @NotBlank  // null + "" + " "(공백) 까지 걸러냄 ✅✅

&nbsp;   private String name;

}



## **매핑 vs 바인딩**

**매핑   →  전화 교환원 ("이 번호는 이 부서로 연결")**

**바인딩 →  직원이 메모지에 내용 받아 적기**



**요청 매핑 어노테이션**



@RequestMapping

HTTP 요청을 특정 메서드나 클래스에 매핑하는 어노테이션입니다

매핑  →  "어떤 URL 요청을 어떤 **메서드**가 받을지" 연결



**요청 파라미터 바인딩**

HTTP 요청으로 들어온 데이터를 

Java 메서드 파라미터에 자동으로 연결해주는 것입니다.

바인딩 →  "요청 데이터를 **메서드 파라미터**에 넣어주는" 것



| 어노테이션 | 데이터 위치 | 주요 용도 |

|---|---|---|

| `@PathVariable` | URL 경로 | 리소스 ID |

| `@RequestParam` | 쿼리 스트링 | 검색 조건, 페이징 |

| `@RequestBody` | HTTP Body (JSON) | REST API 데이터 수신 |

| `@ModelAttribute` | HTTP Body (Form) | HTML Form 수신 |

| `@RequestHeader` | HTTP 헤더 | 토큰, 언어 설정 |



REST API 개발에서는



적은 데이터  →  @RequestParam

많은 데이터  →  @RequestBody + DTO

이 두 가지 패턴이 대부분입니다.



## **RequestParam vs PathVariable**

// @PathVariable — "1번 유저 줘"

GET /users/1



// @RequestParam — "Kim이라는 이름 가진 유저 검색해줘"

GET /users?name=Kim



**@PathVariable  →  URL로 리소스 하나를 콕 집을 때  (/users/1)**

**@RequestParam  →  조건으로 검색/필터링할 때  (?name=Kim)**

**@RequestBody   →  생성/수정할 데이터를 담을 때 (JSON Body)**



## **커맨드 객체 vs DTO**

**DTO (Data Transfer Object)**

    **│**

    **│  요청 데이터를 담는 객체 전부**

    **│**

    **├── 커맨드 객체  (@ModelAttribute, Form 데이터)** @NoArgsConstructor + @Setter 

    **└── 그냥 DTO    (@RequestBody, JSON 데이터)** @NoArgsConstructor + @Getter







커맨드 객체에서 @AllArgsConstructor 단독 사용→  기본 생성자 자동으로 생성하지 못하므로, 사용하지 않음

그러나 그냥 dto에서는 jackson이 처리하므로 사용함



파일 업로드는 Form 데이터(multipart/form-data) 형식으로 전송되기 때문에 

JSON이 아닌 Form 데이터를 처리하는 @ModelAttribute 방식을 씁니다.



### **DTO에서 쓰는 롬복 어노테이션** 

1. **@NoArgsConstructor, @Getter 왜 쓰나?**

Jackson이 JSON → 객체 변환할 때 기본적으로 빈 객체를 먼저 만들기 때문입니다.

@NoArgsConstructor  →  역직렬화 (JSON → 객체) 에서만 필요 /  객체 생성용

@Getter             →  직렬화   (객체 → JSON) 에서만 필요 / 값 읽기용



2\. 	 **전체 필드 생성자 @AllArgsConstructor 왜 쓰나?**



차이점

Request  →  @AllArgsConstructor 없어도 됨 (Jackson이 채워주니까)

Response →  @AllArgsConstructor 필요 (서비스에서 직접 객체 만들어야 하니까)



// Response는 서비스에서 이렇게 직접 생성

UserResponse response = new UserResponse("Kim", 20); // AllArgsConstructor 필요

return response;



서비스에서 객체 만들 때 필요

@Builder와 같이 쓰기 위해서입니다.



// @Builder만 있으면 내부적으로 전체 필드 생성자가 필요

UserRequest.builder()

&nbsp;   .name("Kim")

&nbsp;   .age(20)

&nbsp;   .build();  // 내부에서 AllArgsConstructor 호출



**3. 	@Setter를 DTO에 쓰면 안 되는 이유**

불변성(Immutability) 때문입니다

DTO는 요청 데이터를 담아서 전달하는 역할이기 때문에 중간에 값이 바뀌면 안 됩니다.



## **@RequestBody**

요청데이터인 JSON 객체를 jackson의 ObjcetMapper로 DTO 객체로 매핑해줌



###### @PostMapping("/v4")

###### &nbsp; public void requestBody(@RequestBody UserRequest request){

###### &nbsp;   System.out.println(request);

###### &nbsp; }

@RequestBody 는 요청 본문에서 꺼내서 dto에 넣으라는 의미



< 요청 바디 종류>

* form 태그 ->  x-www-form-urlencoded
* file 첨부 양식 -> form-data
* raw -> 원본





# **@ResponseBody**

&nbsp;   

컨트롤러 환경에서 화면(HTML)으로 전환하지 않고, 자바 데이터를 HTTP Response Body에 담아서 반환하는 것

요청이 온 곳으로 데이터를 응답하는 것을 의미함 (즉, 요청 페이지는 변하지 않음)

클라이언트는 비동기 요청을 이용해야만 정상적으로 데이터를 받아서 처리할 수 있음





컨트롤러 메서드에 `@ResponseBody` 어노테이션을 붙여줌

이 어노테이션이 붙으면 반환 값을 `ViewResolver`로 처리하지 않고, `HttpMessageConverter`로 처리함

자바 객체(DTO)를 반환하면, Jackson 라이브러리가 작동하여 이를 JSON 문자열로 자동 변환한 뒤 브라우저로 전달함



@ResponseBody 작동 순서



1\. 메서드 반환값 확인

2\. 클라이언트 Accept 헤더 확인

3\. 적절한 HttpMessageConverter 선택

4\. ObjectMapper로 JSON 직렬화

5\. HTTP Response Body에 작성



@RestController = @Controller + 모든 메서드에 @ResponseBody



### **ResponseBody가 메소드마다 100프로 필요한 이유**

**백엔드 단에서 view를 반환하지 않기 때문에**



## **@Controller vs @RestController**

**@Controller**

— View를 반환

"home"이라는 문자열을 반환하면 Spring이 home.html을 찾아서 HTML 페이지로 응답합니다.



**@RestController**

— 데이터를 반환

Dto 객체를 반환하면

ObjectMapper가 자동으로 JSON으로 직렬화해서 응답합니다.



**@Controller에서 JSON 반환하고 싶다면?**

@ResponseBody를 메서드마다 붙여야 합니다.

@RestController는 이 @ResponseBody를 클래스 전체에 미리 붙여놓은 것과 동일합니다.



요즘은 프론트엔드(React 등)와 백엔드를 분리하는 구조(CSR - JS가 API 호출) RestController를 훨씬 많이 씁니다.

@Controller는 Thymeleaf 같은 서버 사이드 템플릿 엔진을 쓸 때 주로 사용해요. (SSR-서버가 html 생성)



# **Record**

// java 16+ 정식으로 체택된 Record

// 1. 모든 필드가 final 처리 (데이터 불변, setter 불가)

// 2. 컴파일러가 필요한 코드를 자동으로 생성 (생성자, tosting)

// 3. getter 자동 생성, 단 get으로 시작하지 않음 필드명과 동일함

// 4. 빌더패턴과의 콜라보도 가능!



>> 요청과 응답 전송 객체에 레코드를 쓰는 것을 권장됨



# **ResponseEntity<T>**

@ResponseBody나 @RestController`를 이용해 자바 객체를 JSON으로 반환할 수 있음



&nbsp;이 방식은 성공 시 항상 `200 OK` 상태코드만 반환함

&nbsp;실무는 더 많은 상태코드가 필요함

&nbsp;새로 생성되면 `201 Created`

&nbsp;클라이언트의 입력이 잘못되면 `400 Bad Request`

&nbsp;권한 없는 사용자가 접근하면 `403 Forbidden`



**http 상태코드**



200  →  정상



201  →  생성 성공



204  →  삭제 성공



301 -> 우리 이사했어요



302 -> 임시 공간



400  →  내가 요청 잘못 보냄



401  →  로그인 필요 (너누구야)



403  →  권한 없음 (권한 없어)



404  →  없는 URL



500  →  서버 터짐



502  →  Nginx-WAS 연결 문제



503  →  서버 죽음



# **MVC 패턴**

#### **<mvc 패턴이란?>**



Model       →  데이터 + 비즈니스 로직

View        →  화면 (HTML, JSP)

Controller  →  Model과 View 연결



**전통적 MVC          Spring Boot**

&nbsp;                 

Model       →       Service    (비즈니스 로직) + Repository (DB 접근) + Entity     (데이터)



View        →       React, Vue 등 (프론트엔드) + 또는 Thymeleaf (서버사이드)



Controller  →       Controller (그대로)



---



DAO (Data Access Object)  →  전통적인 Java 용어

Repository                →  Spring Data JPA 용어



→ 둘 다 DB에 접근하는 역할로 거의 같은 개념



---



#### **왜 모델이 세분화 되었나?**



Model이 너무 많은 역할을 담당하면 코드가 복잡해짐



비즈니스 로직  →  Service로 분리

DB 접근       →  Repository로 분리

데이터        →  Entity로 분리



→ 각 클래스가 하나의 역할만 담당 (단일 책임 원칙)



## **SPRING과 객체지향 설계**



**(s)단일 책임 원칙 → "하나의 클래스는 하나의 역할만 가져라"**

class UserService { void createUser() { ... } }   // 유저 관련만 ✅

class EmailService { void sendEmail() { ... } }   // 이메일 관련만 ✅



**(o)개방 폐쇄 원칙  →  "확장할 때 기존 코드 수정하지 마라"**

class PushService implements NotificationService {}



**(L)리스코프 치환 원칙 → "자식 클래스는 부모 클래스를 대체할 수 있어야 한다"**

NotificationService service = new SmsService();  // 부모타입으로 자식 받아도

service.send("hello");                           // 정상 동작해야 함 ✅



class BadSmsService implements NotificationService {

&nbsp;   public void send(String message) {

&nbsp;       throw new Exception("지원 안 함"); // 부모 역할 못함 ❌

&nbsp;   }

}



**(I)인터페이스 분리 원칙 → "하나의 큰 인터페이스보다 작게 분리해라"**

**(D)의존성 역전 원칙 →  "구체 클래스 말고 인터페이스에 의존해라"**

private final NotificationService notificationService; // 인터페이스 ✅

private final SmsService smsService;                   // 구체 클래스 ❌

