## 자바 입출력



#### 파일 클래스



OS에 따른 경로 구분자

자바는 플랫폼 독립적 동작을 지향하므로, 경로 구분자를 /를 이용해도 문제 없이 인식함

단 (자바 내 문자열 인식을 위해서는 \\\\ 사용 escape 문자!)

windows만 linux, macos와 달리 역슬래쉬를 사용함



File 클래스는 특정 디렉토리에 파일 객체를 생성하는 기능만 함

파일 '내용'을 쓰거나 읽는 기능은 없음

파일 메타데이터를 보거나, 디렉토리 만들고 삭제





file.listFiles()

file.isFile()



try-with-resources문

-> close 자동으로 해줌

-> 예외처리 해야하는 문장을 try 옆 괄호에 넣어줌



**FileOutputStream**

try(FileOutputStream fos = new FileOutputStream(file,true))



true -> 추가모드 on

=> 덮어쓰기 (새로 만들기)의 반대

=> 옆에 추가!



단점: 성능이 안좋아~

실무에서는 **buffer**를 끼워서 씀

###### 중간 메모리 버퍼 쌓아두고 **"한번에"** 내보낸다!!

입출력 = 속도가 매우 느린편

\*\*\* 버퍼 \*\*\*\*에 가득 싣어서 한번에 내보냄~~~~

실무에서 씀~~~



< 하는 방법>
BufferedOutputStream에 인자로 FileOutputStream을 new해서 넣음!

BufferedOutputStream에 인자로 FileOutputStream을 new해서 넣음!



읽어들이는게 어려운 이유 -> 다 읽었는지 검사 (순회)!

byte 값이 -1이 넘어올때까지 계속 반복문 돌리기

텍스트라면 문자, 이미지라면 픽셀 데이터, 어떤 파일이든 바이트 단위 숫자로 읽힙니다.

read()의 반환값은 파일에서 읽은 바이트 1개를 정수로 표현한 것입니다.

char나 String이 아니라 날것의 숫자예요.



'A' 입력

  ↓

아스키 약속표 참조 → 65

  ↓

65를 2진수로 변환 → 01000001

  ↓

파일/메모리에 저장

파일에서 읽기

  ↓

01000001 읽음

  ↓

10진수로 → 65

  ↓

아스키 약속표 참조 → 'A'

---



버퍼는 고정 크기, 매번 덮어씌워짐

        ↓

마지막 회차에 버퍼가 꽉 안 찰 수 있음

        ↓

이전 회차 데이터가 버퍼에 잔재로 남음

        ↓

n (실제 읽은 수)으로 잘라내야 진짜 데이터



read(byte\[])의 반환값은 "버퍼 중 진짜 데이터가 몇 바이트인지" 를 알려주는 값입니다.

n을 무시하고 버퍼 전체를 쓰면 잔재 데이터가 섞여 들어와요.



---

read()       → 바이트 1개를 int로 반환

read(byte\[]) → 바이트 여러 개를 배열에 담고, 몇 개 읽었는지 int로 반환



--

만약 한글 안깨지는 것 보고 싶으면-> 리더 라이터를 씀

 BufferedReader / BufferedWriter 사용!

시스템의 설정이나 지정된 인코딩(UTF-8, EUC-KR 등)에 맞춰 바이트를 문자로 변환함



--

문자가 아니라면 모두 byte 스트림이다.

영어는 꼭 리더 라이터 안해도 되는데 (아스키코드 존재)

한글은 그런거 없어서 꼭 리더 라이터 써줘야 함.



byte\[] b = {97, 112, 112, 108}; // a, p, p, l



new String(b, 0, 4); // "appl" ← 0번부터 4개

new String(b, 1, 2); // "pp"   ← 1번부터 2개

new String(b, 2, 2); // "pl"   ← 2번부터 2개



offset = 배열의 어디서부터 읽을지 (시작 인덱스)

length = 몇 개를 읽을지



---

네트워크와 관련된 모두

서버 네트워크에서 받아오는 것은 바이트 스트림만 가능



--

while((readByte = bis.read()) != -1){

            bos.write(b,0,readByte);

          }

b배열에 대해 0부터 readbyte까지 내보냄 (bis.read() 읽어낸 바이트)

