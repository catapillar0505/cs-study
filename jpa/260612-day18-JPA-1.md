# JPA ( 자바 영속성 API )

- ORM 기술 사용하는 인터페이스
- JPA의 대표적 구현체 = 하이버네이트

---

### 엔티티

`DB 테이블과 1:1 매핑되는 영속성 객체`

### 영속성이란?

**왜 "영속"이라는 이름이 붙었나**

```java
Product product = new Product("키보드", 50000);
```

이렇게 만든 객체는 **메모리(RAM)**에 있어요. 메모리는 프로그램이 종료되거나 서버가 꺼지면 데이터가 **사라지는** 휘발성(volatile) 공간이죠.

```java
em.persist(product);
tx.commit();
```

반면 DB(디스크)에 저장된 데이터는 프로그램이 꺼져도, 서버를 재시작해도 **계속 남아있어요**. 그래서 "지속된다", "영속화된다"라고 부르는 거예요.

**비유로 설명하면**

- **메모리에만 있는 객체** = 칠판에 분필로 쓴 메모. 교실 불 끄고 나가면(프로그램 종료) 누가 지우개로 지우거나, 그냥 사라질 수도 있음
- **영속화(persist)된 객체** = 그 메모를 종이에 옮겨 적어서 캐비닛(DB)에 보관. 교실 불을 꺼도(프로그램이 꺼져도) 캐비닛 안의 종이는 그대로 남아있음

### 영속성 컨텍스트

```
영속화될 객체들을 관리하는 작업 공간이라는 의미예요.
`persist()`라는 메서드 이름 자체가 "이 객체를 영속 상태로 만들어라"
= "이 객체를 DB에 저장될 대상으로 등록해라"라는 뜻이죠.
```

**정리**

| 상태               | 영어    | 의미                                                |
| ------------------ | ------- | --------------------------------------------------- |
| Transient (비영속) | -       | 메모리에만 존재, 프로그램 종료 시 사라짐            |
| Persistent (영속)  | Persist | DB에 저장되어 지속적으로 존재하게 될 (또는 된) 상태 |

> "이 데이터가 프로그램의 생명주기와 무관하게 계속 존재한다"는 의미를 담고 있는 거예요.

> 자바 객체의 일시적인 생명주기를 넘어서 "지속되는" 데이터로 만든다는 뜻에서 붙여진 이름입니다.

# JPA 영속성 컨텍스트 & 캐시 매핑 관계

```
영속성 유닛 (persistence unit)
  └── EntityManagerFactory (1개)
        ├── 2차 캐시 (1개, EMF 전체 공유)
        │
        ├── EntityManager (N개, 보통 트랜잭션당 1개)
        │     └── 영속성 컨텍스트 (1개)
        │           └── 1차 캐시 (1개)
        │
        ├── EntityManager (N개, 보통 트랜잭션당 1개)
        │     └── 영속성 컨텍스트 (1개)
        │           └── 1차 캐시 (1개)
        │
        └── ... (트랜잭션마다 반복)
```

### JPA의 캐시

**1차 캐시 (영속성 컨텍스트)**

`EntityManager`가 트랜잭션 내에서 관리하는 캐시
같은 엔티티를 두 번 조회하면 두 번째부터는 DB에 안 가고 메모리에서 바로 꺼내줌

```java
Member member1 = em.find(Member.class, 1L); // DB 조회
Member member2 = em.find(Member.class, 1L); // 캐시에서 반환, SQL 안 나감
```

같은 트랜잭션 안에서 같은 엔티티를 여러 번 참조하는 로직이라면
DB 왕복 횟수가 줄어드니 성능에 도움이 되죠.

**2차 캐시 (애플리케이션 전역)**

트랜잭션이 끝나도 유지되는 캐시예요.
여러 사용자가 같은 데이터를 자주 조회한다면(예: 카테고리 목록, 공통 코드 같은 거의 안 바뀌는 데이터),
첫 조회 이후엔 DB를 거치지 않고 캐시에서 응답할 수 있어요.
Ehcache, Redis 같은 외부 캐시와 연동해서 씁니다.

**"트랜잭션 처리 시간 단축"이라는 표현에 대해**

엄밀히 말하면 1차 캐시는 "같은 트랜잭션 내 중복 조회"를 줄여주는 거고,
2차 캐시는 "전체 시스템의 DB 부하"를 줄여주는 거예요. 둘 다 결과적으로 응답 시간이 줄어드는 효과는 있지만,
"트랜잭션 시간 단축"이라는 표현은 1차 캐시 쪽 설명이 더 정확해요.

**면접 꼬리질문 포인트**

2차 캐시는 여러 트랜잭션/사용자가 공유하기 때문에, 데이터가 자주 변경되는 엔티티에 잘못 적용하면 오히려 정합성 문제(stale data)가 생길 수 있어요.
그래서 읽기 전용이거나 거의 안 바뀌는 데이터에만 신중하게 적용하는 게 일반적입니다.

### Dialect가 하는 일

```
1) 개발자는 JPQL로 select m from Member m처럼 DB에 종속되지 않은 코드를 작성
2) Hibernate가 application.yml에 설정된 Dialect(예: MySQLDialect, OracleDialect)를 확인
3) 해당 DB에 맞는 실제 SQL로 번역
```

---

#### <4가지 생명주기>

1. 생성되었으나 관리 안됨(비영속)

- JAVA 객체

2. 관리됨(영속)

- 1차캐시에 저장됨
- 더티 캐시 적용
- 쓰기지연 SQL 저장소
- 지연 로딩

3. 분리됨(준영속)
4. 삭제됨(삭제)

---

**흐름 단계별로**

```java
Product product = new Product("키보드", 50000);
// 1단계: 비영속 상태 (Transient)
//   - 그냥 메모리에 떠 있는 일반 객체. JPA가 모르는 상태.

em.persist(product);
// 2단계: 영속 상태 (Persistent)
//   - 영속성 컨텍스트(1차 캐시)에 등록됨
//   - 하지만 아직 INSERT SQL은 DB로 안 나감!

tx.commit();
// 3단계: 커밋 시점에 flush 발생
//   - 영속성 컨텍스트에 쌓인 변경 내용을 DB에 반영
//   - 이때 실제 INSERT SQL이 실행됨
```

# GenerationType 4가지

| 전략       | 동작 방식                              | 주요 사용 DB           |
| ---------- | -------------------------------------- | ---------------------- |
| `IDENTITY` | DB의 AUTO_INCREMENT/IDENTITY 컬럼 사용 | MySQL                  |
| `SEQUENCE` | DB의 시퀀스(Sequence) 객체 사용        | Oracle, PostgreSQL, H2 |
| `TABLE`    | 키 생성을 위한 별도 테이블 사용        | 모든 DB (호환성용)     |
| `AUTO`     | JPA가 DB 종류에 맞춰 자동 선택         | -                      |

**1. `SEQUENCE` - 오라클에서 주로 쓰는 전략**

맞아요, 전통적으로 Oracle은 `AUTO_INCREMENT` 같은 컬럼 자동 증가 기능이 없어서(12c부터 IDENTITY 추가됨), 대신 **시퀀스(Sequence)라는 독립적인 객체**를 만들어서 번호를 뽑아 썼어요.

```sql
CREATE SEQUENCE product_seq START WITH 1 INCREMENT BY 1;
```

```java
@Id
@GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "product_seq")
@SequenceGenerator(name = "product_seq", sequenceName = "PRODUCT_SEQ", allocationSize = 1)
private Long id;
```

앞서 설명한 "번호표 뽑는 기계"가 바로 이 시퀀스 객체예요. 테이블과 별개로 독립적으로 존재하고, `nextval`을 호출하면 INSERT 없이도 다음 번호를 미리 받아올 수 있죠.

**2. `TABLE` - 모든 DB에서 동작하는 범용 전략**

```sql
CREATE TABLE my_sequences (
    sequence_name VARCHAR(255),
    next_val BIGINT
);
```

DB에 시퀀스나 AUTO_INCREMENT가 없어도, **키 값을 관리하는 전용 테이블**을 따로 만들어서 거기서 번호를 가져와요. "어떤 DB에서든 동작해야 한다"는 호환성을 위한 전략인데, 별도 테이블에 접근해야 해서 성능은 가장 떨어져요. 실무에서는 거의 안 씁니다.

**3. `AUTO` - "알아서 골라줘"**

```java
@Id
@GeneratedValue(strategy = GenerationType.AUTO) // 또는 @GeneratedValue만 써도 AUTO가 기본값
private Long id;
```

JPA 구현체(Hibernate)가 현재 설정된 Dialect를 보고 "이 DB에는 어떤 전략이 적합한지" 자동으로 선택해줘요.

- MySQL → `IDENTITY`
- Oracle → `SEQUENCE`
- 등등...

**비유로 정리**

| 전략       | 비유                                                                                          |
| ---------- | --------------------------------------------------------------------------------------------- |
| `IDENTITY` | 은행 창구 직원이 업무 끝내야 번호 알려줌                                                      |
| `SEQUENCE` | 입구에 독립적인 "번호표 뽑기 기계"가 따로 있음                                                |
| `TABLE`    | 번호표 기계가 고장나서, 옆 사무실에 가서 "다음 번호 뭐예요?"하고 종이에 적힌 걸 받아옴 (느림) |
| `AUTO`     | "은행마다 사정이 다르니, 은행 매니저(Hibernate)가 알아서 적절한 방식을 골라줄게"              |

**실무 팁**

`SEQUENCE` 전략은 `allocationSize`를 조절해서 한 번에 여러 개의 PK를 미리 받아올 수 있어요(예: `allocationSize=50`이면 시퀀스에서 한 번에 50개를 확보). 이러면 매번 DB에 시퀀스 호출을 안 해도 되니 성능이 좋아지죠.

# 전략 정리

| 영역      | 용어                      | 의미                                                                     |
| --------- | ------------------------- | ------------------------------------------------------------------------ |
| DB(MySQL) | `AUTO_INCREMENT`          | DB 자체의 기능. 컬럼에 INSERT 시 자동으로 1씩 증가하는 값을 채워줌       |
| JPA       | `GenerationType.IDENTITY` | "이 엔티티의 PK 생성을 DB의 자동 증가 기능에 맡기겠다"는 JPA의 전략 선언 |

```java
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;
```

이 코드는 "이 테이블의 `id` 컬럼은 DB에서 `AUTO_INCREMENT`로 설정되어 있고, JPA는 그 값을 그대로 받아서 쓸게"라는 뜻이에요.

**DB마다 용어가 다름**

재밌는 건, "자동 증가"라는 기능 자체는 DB마다 이름이 달라요:

| DB            | 자동 증가 기능 이름   |
| ------------- | --------------------- |
| MySQL         | `AUTO_INCREMENT`      |
| PostgreSQL    | `SERIAL` / `IDENTITY` |
| Oracle (12c+) | `IDENTITY`            |
| SQL Server    | `IDENTITY`            |

JPA의 `GenerationType.IDENTITY`는 이 모든 DB별 "자동 증가 컬럼" 기능을 **추상화한 이름**이에요. 앞서 얘기했던 Dialect가 여기서도 활약하는데, JPA가 `IDENTITY` 전략을 쓰면 Dialect가 "이 DB에서는 자동 증가를 이렇게 설정해"라고 실제 DDL(`AUTO_INCREMENT` vs `IDENTITY` 등)로 번역해줍니다.

MySQL 기준으로는 "`GenerationType.IDENTITY` = `AUTO_INCREMENT`를 쓰겠다는 JPA의 선언"이라고 이해하면 거의 정확해요. 다만 다른 DB를 쓰면 그 DB의 자동 증가 기능으로 매핑된다는 점만 기억하면 됩니다.

---

# # 핵심 포인트: `persist()` ≠ 즉시 INSERT

`persist()`를 호출하는 순간 바로 SQL이 나가는 게 아니에요.
영속성 컨텍스트에 "쓰기 지연 SQL 저장소"라는 게 있어서, 거기에 INSERT 쿼리를 쌓아두고 있다가
`commit()` 시점(또는 `flush()` 호출 시점)에 한꺼번에 DB로 보냅니다.

단, `@GeneratedValue(strategy = GenerationType.IDENTITY)`를 쓰는 경우는 예외예요.
IDENTITY 전략은 DB가 PK를 생성해야 하기 때문에,
`persist()` 호출 즉시 INSERT가 실행됩니다
(PK 값을 알아야 영속성 컨텍스트에서 식별할 수 있으니까요).

**정리하면**

| 단계                  | 상태   | SQL 실행 여부                             |
| --------------------- | ------ | ----------------------------------------- |
| `new Product()`       | 비영속 | X                                         |
| `em.persist(product)` | 영속   | 보통 X (IDENTITY 전략은 예외로 즉시 실행) |
| `tx.commit()`         | -      | O (flush 발생)                            |

이 "쓰기 지연" 메커니즘은 면접에서 자주 물어보는 포인트예요.

# 쓰기 지연이 적용 되지 않는 상황 (IDENTITY 전략)

    @Test
    @DisplayName("1차 캐시 테스트")
    void identityAndCacheTest() {
    	// 새 엔티티 생성 - 엔티티 매니저가 관리하는 엔티티가 아님 (비영속)
    	Book book = new Book("등촌 칼국수의 비밀", "볶음밥에 국물 3국자");

    	// 엔티티 관리 시작 (영속 상태의 엔티티는 1차 캐시에 저장된다.)
    	em.persist(book);

    	// 엔티티 조회하기
    	// find()메서드: 오직 id 이용해서만 조회 => (1) 1차 캐시에 찾기 (2) 없으면 db 조회
    	Book findBook1 = em.find(Book.class, book.getId());
    	Book findBook2 = em.find(Book.class, book.getId());

    	// 주소 비교를 통해 동일한 엔티티인지 확인
    	assertTrue(findBook1 == findBook2);

    }

재미있는 비유로 설명해볼게요!

**번호표 뽑기 비유**

은행이나 병원에 가면 번호표를 뽑잖아요. 이 번호표가 바로 **PK(Primary Key, 고유 번호)**예요.

**상황 1: 번호표 뽑기 기계가 따로 있는 경우 (SEQUENCE)**

```
🎫 번호표 뽑기 기계 (시퀀스)
        ↓
"42번입니다!" (번호만 미리 받음)
```

은행에 들어가자마자 입구에 있는 번호표 기계에서 "42번" 표를 뽑아요. 아직 창구(DB)에 가서 실제 업무(INSERT)를 보지는 않았지만, **번호는 이미 손에 있죠**.

그래서:

- "어, 42번 손님 오셨네!" (영속성 컨텍스트에 등록 가능 ✅)
- 근데 실제 업무 처리(INSERT)는 좀 이따가 한꺼번에 처리해도 됨 (쓰기 지연 가능 ✅)

**상황 2: 번호표 기계가 없는 경우 (IDENTITY)**

```
창구 직원이 업무를 끝내야만
"네, 손님 번호는 17번입니다!"
```

이 은행은 번호표 기계가 없어요. **창구 직원(DB)이 업무를 다 처리해야만** "당신은 17번 손님이었어요"라고 알려줘요.

그래서:

- "이 사람이 몇 번 손님인지" 알려면 → 일단 업무(INSERT)부터 끝내야 함
- 업무를 미룰 수가 없음! (쓰기 지연 불가능 ❌)

**핵심 요약**

|                             | 번호(PK)를 언제 알아? | 업무(INSERT) 미룰 수 있어? |
| --------------------------- | --------------------- | -------------------------- |
| SEQUENCE (번호표 기계 있음) | 업무 전에 미리        | 가능 👍                    |
| IDENTITY (번호표 기계 없음) | 업무 끝나야           | 불가능 👎                  |

즉, **"네가 몇 번인지 알려면 일을 끝내야 해"**(IDENTITY) vs **"넌 미리 42번이야, 일은 나중에 처리해도 돼"**(SEQUENCE)의 차이예요!

---

# PK 생성 전략과 생성자 설계

이건 **PK를 누가 만드느냐**의 차이에서 나오는 이야기예요.

**PK 생성 전략이 있을 때 (`@GeneratedValue`)**

```java
@Id
@GeneratedValue(strategy = GenerationType.IDENTITY)
private Long id;
```

이 경우 `id`는 **DB가 만들어주는 값**이에요. 그래서 객체를 생성하는 시점에는 `id` 값을 알 수 없어요.

```java
// AllArgsConstructor라면?
public Book(Long id, String title, String author) { ... }

new Book(?, "이펙티브 자바", "조슈아 블로크");
// id에 뭘 넣어야 하지? null? 1L? 둘 다 의미 없음!
```

`id`에 넣을 의미 있는 값이 없으니, `id`를 매개변수로 받는 생성자(`AllArgsConstructor`) 자체가 어색해져요. 그래서:

```java
public Book(String title, String author) { // id 제외!
    this.title = title;
    this.author = author;
}
```

이렇게 **id를 제외한 생성자**를 직접 만들어 쓰는 거죠.

---

**PK 생성 전략이 없을 때 (직접 할당)**

```java
@Id
private String id; // @GeneratedValue 없음! 개발자가 직접 값을 정함
```

이 경우는 **개발자가 객체를 만드는 시점에 id 값을 직접 결정**해요. 예를 들어:

```java
@Entity
public class Product {
    @Id
    private String productCode; // "P001", "P002" 같은 비즈니스 코드

    private String name;
    private int price;
}
```

`productCode`는 DB가 자동으로 만들어주는 게 아니라, 개발자(또는 비즈니스 로직)가 "P001" 같은 값을 정해서 넣는 거예요. 그러니까:

```java
@AllArgsConstructor // id 포함해도 자연스러움!
public class Product {
    private String productCode;
    private String name;
    private int price;
}

new Product("P001", "노트북", 1500000); // id가 의미 있는 값으로 채워짐 ✅
```

이때는 `id`를 포함한 `AllArgsConstructor`가 전혀 이상하지 않아요. 왜냐하면 **id도 다른 필드들처럼 "생성 시점에 결정되는 일반적인 값"**이기 때문이에요.

---

**비유로 정리: "번호표 vs 미리 정해진 자리"**

| 상황                                  | 비유                                                                                                  |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| `@GeneratedValue` (IDENTITY/SEQUENCE) | 영화관 입장 시 **번호표를 뽑아야** 좌석을 알 수 있음 → 입장 전(생성자 호출 시점)에는 좌석 번호를 모름 |
| PK 생성 전략 없음 (직접 할당)         | **예매할 때 이미 좌석을 지정**해서 옴 → 입장 전(생성자 호출 시점)에 이미 좌석 번호를 알고 있음        |

번호표를 뽑아야 하는 상황에서는 "너 몇 번 좌석이야?"라고 미리 물어볼 수 없죠(아직 안 뽑았으니까). 반대로 이미 좌석을 지정해서 온 사람에게는 "몇 번 좌석이세요?"가 자연스러운 질문이고요.

---

**정리표**

|                                 | `@GeneratedValue` 있음                | PK 생성 전략 없음                |
| ------------------------------- | ------------------------------------- | -------------------------------- |
| ID는 누가 정함?                 | DB (INSERT 시점)                      | 개발자/비즈니스 로직 (생성 시점) |
| 생성자 호출 시 ID 값?           | 알 수 없음 (없음)                     | 이미 알고 있음                   |
| `AllArgsConstructor`에 id 포함? | 부자연스러움 (id 뺀 생성자 따로 만듦) | 자연스러움                       |

**한 줄 요약**

"`@GeneratedValue`가 있으면 **id는 '나중에 DB가 채워줄 빈칸'**이라서 생성자에 넣을 수 없고, 없으면 **id도 '다른 필드처럼 처음부터 내가 정하는 값'**이라서 `AllArgsConstructor`에 자연스럽게 포함될 수 있는 거예요."

---

"Flush"는 일반 영어로 "한꺼번에 씻어내리다, 쏟아내다"라는 뜻이에요. 화장실 변기의 "물 내리기(flush)"를 떠올리면 가장 직관적이에요.

**"Flush"의 일반적인 의미**

- 화장실 변기 물 내리기 (flush the toilet)
- 얼굴이 갑자기 붉어지다 (flush with embarrassment)
- 컴퓨터 분야: 버퍼(buffer)에 쌓여있던 데이터를 한꺼번에 실제 목적지로 내보내는 것

공통점은 "무언가 쌓여있던 것을 한 번에 비우고 내보낸다"는 이미지예요.

**JPA의 flush와 연결하기**

영속성 컨텍스트 안에는 "쓰기 지연 SQL 저장소"라는 게 있어요. 여기에 INSERT, UPDATE, DELETE 쿼리들이 **임시로 쌓여(buffered)** 있죠.

```java
em.persist(product1); // INSERT 쿼리가 저장소에 "쌓임" (아직 DB로 안 감)
em.persist(product2); // 또 쌓임
product3.setName("수정"); // UPDATE 쿼리도 쌓임

// 이 시점까지는 DB에 아무것도 반영 안 됨!

tx.commit(); // flush 발생 → 쌓여있던 쿼리들이 한꺼번에 DB로 "쏟아져 나감"
```

**비유로 설명하면**

영속성 컨텍스트의 쓰기 지연 저장소는 "변기 물탱크"와 비슷해요. 물을 쓸 때마다(persist, update) 탱크에 물이 차오르지만, 실제로 변기 아래로 내려가는 건 "내리기 버튼(flush)"을 누르는 순간이에요. `commit()`이나 `flush()`를 호출하는 게 바로 그 버튼을 누르는 행위인 거죠.

**일반 컴퓨터 용어에서의 buffer ↔ flush 관계**

이건 JPA만의 개념이 아니라 IO 분야 전반에서 쓰이는 패턴이에요:

```java
BufferedWriter writer = new BufferedWriter(...);
writer.write("hello"); // 메모리 버퍼에 쌓임
writer.flush(); // 버퍼 내용을 실제 파일/스트림으로 내보냄
```

"버퍼에 쌓아두다가 → flush로 한 번에 내보낸다"는 패턴이 동일하죠.

**정리**

| 용어                          | 의미                             | JPA에서의 역할                                                            |
| ----------------------------- | -------------------------------- | ------------------------------------------------------------------------- |
| Buffer (쓰기 지연 SQL 저장소) | 임시로 쌓아두는 공간             | persist/update/delete 호출 시 SQL이 여기 쌓임                             |
| Flush                         | 쌓인 것을 한꺼번에 비워서 내보냄 | 쌓인 SQL들을 실제 DB로 전송 (영속성 컨텍스트의 내용은 유지됨, SQL만 전송) |

한 가지 주의할 점은, flush가 일어나도 **영속성 컨텍스트 자체가 비워지는 건 아니에요**. 1차 캐시에 있는 엔티티들은 그대로 남아있고, "DB에 동기화할 SQL들만" 전송되는 거예요. 변기 물탱크 비유로 치면, 물은 내려갔지만 탱크(영속성 컨텍스트)는 다시 채워질 준비가 되어있는 상태인 거죠.

# select 쿼리를 날린다 = DB까지 간다

## 왜 그런가?

**select 쿼리 자체가 "DB에 보내는 요청"이라는 의미**니까. JDBC 레벨에서 보면:

```
Connection → Statement/PreparedStatement → executeQuery() → DB 서버로 네트워크 요청 전송 → DB가 디스크/메모리에서 데이터 조회 → 결과를 다시 네트워크로 반환
```

즉 select 쿼리가 "생성"되고 "실행"된다는 건, JDBC 드라이버를 거쳐서 실제로 **네트워크를 타고 DB 서버한테 물어보러 가는 행위** 자체를 말하는 거야.

## 1차 캐시랑 비교하면

- **1차 캐시에서 찾음** = 영속성 컨텍스트(자바 메모리, 애플리케이션 서버 안)에서 끝남 → DB 서버한테 물어볼 필요가 없음 → select 쿼리 자체가 **만들어지지도 않음**
- **DB까지 감** = JPA가 "캐시에 없네, DB한테 물어봐야겠다" → select 쿼리를 만들어서 DB 서버로 전송 → DB가 응답

## 비유

학교 도서관(DB)에 책을 빌리러 가는 상황으로 생각하면:

- **1차 캐시 히트**: 이미 내 책상 위에 그 책이 펼쳐져 있음 → 도서관 갈 필요 없음 (select 발생 안 함)
- **1차 캐시 미스**: 내 책상에 없음 → 도서관으로 걸어가서(쿼리 전송) 책을 빌려옴(결과 받음) → select 발생

그래서 "select가 안 날아간다"는 말은 정확히 "이 작업이 애플리케이션 메모리(1차 캐시) 안에서 완전히 끝났고, DB 서버와의 통신이 전혀 발생하지 않았다"는 뜻이야.

**한 줄 요약**: select 쿼리 = JPA가 영속성 컨텍스트(1차 캐시) 안에서 답을 찾지 못해서 DB 서버에게 보내는 네트워크 요청 그 자체.

---

# `find()`가 1차 캐시부터 가는 이유

**핵심 이유: "동일성 보장(Identity Guarantee)"**

같은 트랜잭션 안에서, 같은 PK로 조회한 엔티티는 **항상 같은 객체(인스턴스)여야 한다**는 게 JPA의 중요한 보장 사항이에요.

```java
Product p1 = em.find(Product.class, "p001");
Product p2 = em.find(Product.class, "p001");

System.out.println(p1 == p2); // true! (== 비교, 진짜 같은 객체)
```

**왜 이게 중요한가? - 비유로 설명**

학교 도서관에서 "해리포터 1권"을 빌리는 상황을 생각해보세요.

```
김학생이 "해리포터 1권" 대출 신청
→ 사서가 책장에서 책을 꺼내줌 (DB 조회, 책 #A)

이학생이 "해리포터 1권" 대출 신청
→ 사서: "어? 방금 김학생한테 똑같은 책 줬는데?"
→ 만약 창고에서 또 다른 책 #B를 꺼내준다면?
```

만약 매번 새 객체(새 책)를 만들어준다면:

```java
Product p1 = em.find(Product.class, "p001");
p1.setName("수정된 이름A");

Product p2 = em.find(Product.class, "p001"); // 새로운 객체라면?
p2.setName("수정된 이름B");

// commit 시점에 어떤 값이 진짜인가? p1의 "수정된 이름A"? p2의 "수정된 이름B"?
// 같은 데이터를 가리키는 두 개의 다른 객체가 서로 다른 상태를 가지는 모순!
```

**1차 캐시 = "이미 꺼내준 책의 명부"**

```java
Product p1 = em.find(Product.class, "p001");
// 1. 1차 캐시 확인 → 없음
// 2. DB 조회 → SELECT * FROM product WHERE id='p001'
// 3. 조회 결과로 엔티티 생성, 1차 캐시에 저장 (key="p001", value=객체)
// 4. 그 객체 반환

Product p2 = em.find(Product.class, "p001");
// 1. 1차 캐시 확인 → 있음! (key="p001"로 저장된 객체 발견)
// 2. DB 조회 안 함! 캐시에 있던 그 객체를 그대로 반환
// 3. p1 == p2 → true
```

**정리: 1차 캐시가 먼저인 이유 2가지**

1. **성능**: 같은 데이터를 반복 조회할 때 DB 왕복을 줄임
2. **동일성 보장**(더 중요한 이유): 같은 PK = 같은 객체여야 변경 감지(Dirty Checking)나 데이터 일관성이 깨지지 않음

**한 줄 요약**

"이미 빌려준 책을 또 빌려달라고 하면, 창고(DB)에 또 가지 말고 명부(1차 캐시)를 보고 '아까 그 책 그대로 줄게'라고 하는 것" — 이게 1차 캐시가 우선인 이유예요.

---

# 싱글톤 패턴과 레지스트리 메커니즘의 유사성

네, 본질적으로 **"키를 기준으로 한 객체를 등록해두고, 같은 키로 요청하면 새로 안 만들고 기존 걸 재사용한다"**는 패턴 자체는 똑같아요. 좋은 직관이에요!

**공통점: 둘 다 "Map 기반 레지스트리"**

```java
// Spring 컨테이너 (개념적으로)
Map<String, Object> singletonBeans = new HashMap<>();
// key: 빈 이름, value: 빈 인스턴스

// 영속성 컨텍스트의 1차 캐시 (개념적으로)
Map<Object, Object> persistenceContext = new HashMap<>();
// key: (엔티티클래스 + PK), value: 엔티티 인스턴스
```

```java
// Spring
@Autowired
private ProductService productService; // 컨테이너의 Map에서 "꺼내옴", 새로 안 만듦

// JPA
Product p1 = em.find(Product.class, "p001"); // 1차 캐시 Map에서 "꺼내옴" (있으면)
```

둘 다 "이미 있으면 재사용, 없으면 새로 만들어서 등록"이라는 흐름이에요.

**하지만 중요한 차이가 있어요**

|                   | Spring 빈 (싱글톤)                | JPA 1차 캐시                         |
| ----------------- | --------------------------------- | ------------------------------------ |
| **생명주기 범위** | 애플리케이션 전체 (서버 종료까지) | 트랜잭션(영속성 컨텍스트) 1개 동안만 |
| **공유 범위**     | 모든 요청, 모든 사용자가 공유     | 같은 트랜잭션 내에서만 유효          |
| **키**            | 빈 이름/타입 (보통 1개만 존재)    | 클래스 + PK (데이터마다 다름)        |
| **목적**          | 객체 생성 비용 절감, 상태 공유    | 동일성 보장 + DB 조회 최소화         |

**비유로 차이를 강조하면**

- **Spring 싱글톤** = "회사에 직원은 김부장 한 명뿐이야. 누가 찾아도 항상 같은 김부장한테 연결해줘." (전역적, 영구적)
- **1차 캐시** = "오늘 이 회의(트랜잭션) 동안에는, '고객 A 파일'을 한 번 꺼내왔으면 회의 끝날 때까지 그 파일 그대로 써. 회의 끝나면(트랜잭션 종료) 파일은 다시 캐비닛에 들어가." (한정적, 임시적)

**가장 핵심적인 차이 하나만 꼽으면**

Spring 싱글톤 빈은 "**여러 요청에 걸쳐** 계속 같은 객체"지만, 1차 캐시는 "**하나의 트랜잭션 안에서만** 같은 객체"예요. 트랜잭션이 끝나면 1차 캐시도 사라지고, 다음 트랜잭션에서 같은 PK로 `find()`하면 다시 새로운 엔티티 객체가 만들어집니다 (DB에서 새로 조회).

**한 줄 정리**

"**같은 키 → 같은 인스턴스 재사용**"이라는 메커니즘은 동일하지만, Spring은 **애플리케이션 전체 생명주기**, JPA 1차 캐시는 **트랜잭션 단위의 임시 생명주기**라는 점에서 "스코프(scope)"가 다르다고 보면 정확해요.

# 레지스트리

"참조"가 더 상위 개념이고, "레지스트리(키-값 조회)"는 참조를 구현하는 한 가지 방식이야.

**참조(Reference) = 어떤 대상을 가리키는/찾아갈 수 있게 하는 모든 수단**

참조에는 여러 구현 방식이 있어:

| 방식                    | 예시                                 | 설명                                                                        |
| ----------------------- | ------------------------------------ | --------------------------------------------------------------------------- |
| **메모리 주소(포인터)** | Java 객체 변수, C 포인터             | 객체가 메모리상 어디 있는지 직접 가리킴                                     |
| **이름/키 기반 조회**   | Map, DB의 FK, JNDI, Spring Bean 이름 | 레지스트리(중간 저장소)에 이름을 등록해두고, 그 이름으로 실제 객체를 찾아옴 |
| **핸들(handle)**        | 파일 디스크립터, OS 핸들             | 간접 식별자를 통해 실제 리소스에 접근                                       |

**Java 객체 참조의 경우**

```java
Item item = new Item("사과", category);
```

여기서 `item.category`는 **메모리 주소(정확히는 JVM 내부의 객체 참조값)** 로 Category 객체를 직접 가리키는 거야. 레지스트리를 거치지 않고 바로 찾아감.

**DB의 FK(`category_id`)의 경우**

```sql
items.category_id = 5
```

이건 **이름(키) 기반 참조**야. `categories` 테이블이라는 "레지스트리"에서 `id = 5`인 행을 찾아가는 방식이지.

**정리**

- **참조** = "A가 B를 가리켜서, A를 통해 B에 도달할 수 있다"는 _관계 자체_ (상위 개념)
- **주소 기반 참조** = 메모리 위치로 직접 도달 (Java 객체 참조, 포인터)
- **레지스트리(키) 기반 참조** = 중간 저장소에 등록된 키로 간접 도달 (DB FK, Map)

둘 다 "참조"의 하위 구현 방식일 뿐, 레지스트리가 참조의 정의는 아니야.

**한 줄 요약**: 참조는 "어떤 걸 가리켜서 찾아갈 수 있다"는 포괄적 개념이고, 주소 기반(직접 도달)과 키/레지스트리 기반(간접 도달)은 그 구현 방법들이야.

> 이름(키)으로 객체를 찾아갈 수 있도록 등록해두는 저장소

**일상적인 비유: 사물함 / 명찰**

학교 사물함을 떠올려보세요.

- 사물함마다 번호(키)가 있고
- 그 번호로 가면 항상 같은 물건(값/인스턴스)이 들어있어요

```
사물함 1번 → 김철수의 가방
사물함 2번 → 이영희의 가방
```

"1번 사물함 줘"라고 하면 매번 새 가방을 만들어주는 게 아니라, **이미 거기 들어있는 그 가방**을 꺼내주는 거죠. 이게 레지스트리의 핵심 동작이에요.

**프로그래밍에서의 레지스트리 패턴**

```java
// 가장 단순한 형태: Map
Map<String, Object> registry = new HashMap<>();

// 등록 (register)
registry.put("productService", new ProductService());

// 조회 (lookup)
ProductService service = (ProductService) registry.get("productService");
// → 매번 get할 때마다 새로 만드는 게 아니라, 등록된 걸 그대로 줌
```

**핵심 동작 2가지**

1. **등록(Register)**: "이 키로 이 객체를 저장해둬"
2. **조회(Lookup)**: "이 키에 해당하는 객체 줘" → 없으면 새로 만들어서 등록하고 반환, 있으면 기존 거 반환

**우리가 얘기했던 두 사례를 레지스트리 관점으로 다시 보면**

```java
// Spring 컨테이너 = 빈(Bean) 레지스트리
registry: { "productService" → ProductService 인스턴스 }

// JPA 영속성 컨텍스트 = 엔티티 레지스트리 (1차 캐시)
registry: { (Product.class, "p001") → Product 인스턴스 }
```

둘 다 "**키로 조회했을 때 항상 같은 인스턴스를 돌려주는 저장소**"라는 점에서 레지스트리 메커니즘이에요.

**레지스트리 패턴이 쓰이는 다른 예시들**

| 예시                            | 키                 | 값                 |
| ------------------------------- | ------------------ | ------------------ |
| Spring IoC 컨테이너             | 빈 이름/타입       | 싱글톤 빈 인스턴스 |
| JPA 영속성 컨텍스트             | 엔티티 클래스 + PK | 엔티티 인스턴스    |
| 자바 `String` 풀(String Pool)   | 문자열 리터럴 값   | String 객체        |
| Java의 `ConcurrentHashMap` 캐시 | 임의의 키          | 캐시된 값          |

`String` 풀 예시도 흥미로운데:

```java
String a = "hello";
String b = "hello";
System.out.println(a == b); // true!
// 자바가 내부적으로 "hello"라는 키로 문자열 풀(레지스트리)에서 같은 객체를 재사용함
```

**한 줄 정리**

레지스트리 메커니즘 = "**키-값 쌍으로 객체를 등록해두고, 같은 키로 요청하면 새로 만들지 않고 등록된 기존 객체를 그대로 돌려주는 구조**". Spring의 싱글톤 빈 관리, JPA의 1차 캐시, 자바의 String Pool 모두 이 패턴의 변형이라고 보면 됩니다.

---

# 변경 감지(Dirty Checking)를 통한 자동 UPDATE

이게 JPA를 다른 기술(JDBC, MyBatis)과 가장 차별화하는 특징입니다.

**일반적인 데이터 처리 방식 (JDBC/MyBatis 스타일)**

```java
// MyBatis 방식 - "명령형" (어떻게 할지 명시)
public void updateTitle(Long id, String newTitle) {
    bookMapper.updateTitle(id, newTitle); // 명시적으로 UPDATE 쿼리 호출
}
```

```sql
<!-- BookMapper.xml -->
<update id="updateTitle">
    UPDATE books SET title = #{title} WHERE id = #{id}
</update>
```

여기서는 "UPDATE를 실행하라"는 **명령을 직접 내려야** 해요.

**JPA 방식**

```java
@Transactional
public void updateTitle(Long id, String newTitle) {
    Book book = bookRepository.findById(id).orElseThrow(); // SELECT
    book.changeTitle(newTitle); // 객체의 상태만 변경
    // update() 호출 없음! 그런데도 commit 시점에 UPDATE가 나감
}
```

```sql
-- Hibernate가 자동으로 생성
select * from books where id=?
-- (커밋 시점)
update books set author=?, title=? where id=?
```

**여기서 드러나는 JPA의 특징들**

**1. 변경 감지 (Dirty Checking)**

영속성 컨텍스트는 엔티티를 조회한 시점의 **스냅샷(snapshot)**을 저장해둬요. 트랜잭션 커밋 시점에 현재 엔티티 상태와 스냅샷을 비교해서, 변경된 게 있으면 자동으로 UPDATE SQL을 만들어 실행합니다.

```
조회 시점 스냅샷: {id=1, title="자바의 정석", author="남궁성"}
changeTitle() 호출 후: {id=1, title="이펙티브 자바", author="남궁성"}
                              ↑ 다름! → UPDATE 발생
```

**2. 선언적(Declarative) vs 명령형(Imperative)**

|                  | 명령형 (MyBatis)                | 선언적 (JPA)                               |
| ---------------- | ------------------------------- | ------------------------------------------ |
| 개발자가 하는 일 | "UPDATE 쿼리를 실행하라"고 명시 | "객체의 상태를 바꿔라"만 함                |
| SQL 생성 시점    | 개발자가 호출하는 즉시          | 커밋 시점에 JPA가 자동 판단                |
| 비유             | "은행에 직접 가서 서류 제출"    | "수첩에 메모만 해두면, 비서가 알아서 처리" |

이전에 배운 Terraform/Ansible의 "선언적 = 원하는 상태만 기술, 차이는 시스템이 처리" 원리와 정확히 같은 구조예요!

**3. 객체 = DB row의 "살아있는 표현(Living Representation)"**

```java
Book book = bookRepository.findById(id).orElseThrow();
book.changeTitle("새 제목"); // 객체를 바꾸는 게 = DB row를 바꾸는 것처럼 동작
```

JPA에서는 "엔티티 객체를 다루는 것"이 곧 "DB 데이터를 다루는 것"처럼 느껴지게 설계되어 있어요. 개발자는 SQL이 아니라 **객체와 메서드**로 사고하게 됩니다 (객체지향적 접근).

**4. "객체 그래프 탐색"과 결합되면 더 강력해짐**

```java
Order order = orderRepository.findById(id).orElseThrow();
order.cancel(); // 주문 취소
order.getMember().decreasePoint(100); // 회원 포인트도 같이 변경
// 두 엔티티 모두 변경 감지 대상 → 커밋 시 각각 UPDATE 2번 발생
```

여러 엔티티에 걸친 변경도, 각각 `update()` 호출 없이 객체 상태만 바꿔주면 JPA가 알아서 처리해줘요.

**정리: 이 패턴에서 알 수 있는 JPA의 특징**

| 특징                          | 설명                                                                |
| ----------------------------- | ------------------------------------------------------------------- |
| **변경 감지(Dirty Checking)** | 명시적 UPDATE 호출 없이, 스냅샷과 현재 상태 비교로 자동 UPDATE 생성 |
| **선언적 프로그래밍**         | "무엇을 바꿀지"만 표현, "어떻게 SQL을 만들지"는 JPA가 처리          |
| **영속성 컨텍스트의 역할**    | 단순 캐시가 아니라, 변경을 추적하는 "감시자" 역할도 함              |
| **객체 중심 개발**            | SQL이 아닌 도메인 객체와 메서드로 데이터 변경을 표현                |

**한 줄 요약**

"`find()`로 가져온 엔티티는 단순한 데이터 덩어리가 아니라, **JPA가 계속 감시하고 있는 '관리 대상'**이에요. 그래서 `update()`를 호출하지 않아도, 객체의 상태 변화 자체가 곧 'DB를 바꾸겠다는 선언'이 되는 것 — 이게 JPA의 변경 감지(Dirty Checking) 특징입니다."

---

이건 **`changeTitle()`로 한 필드만 변경했어도** Hibernate의 기본 UPDATE 전략 때문에 모든 컬럼이 SQL에 포함되는 거예요. `changeTitle()`을 쓰는 이유와는 별개의 문제입니다.

**왜 모든 컬럼이 UPDATE문에 포함되는가**

Hibernate는 기본적으로 **엔티티의 모든 필드를 포함한 UPDATE문을 미리 만들어둡니다(캐싱)**. 변경 감지(Dirty Checking)는 "어떤 필드가 바뀌었는지"는 감지하지만, 기본 설정에서는 그 정보를 SQL 생성에 반영하지 않아요.

```sql
update books set author=?, title=? where id=?
```

`title`만 바꿨어도 `author`까지 SET절에 포함되는 거죠 (값은 기존과 동일하게 들어감).

**왜 Hibernate는 이렇게 설계했을까?**

**이유 1: SQL 캐싱(재사용) 최적화**

```java
// 매번 같은 모양의 SQL → DB가 이 SQL의 실행 계획(execution plan)을 캐싱해서 재사용
update books set author=?, title=? where id=?  // 항상 동일
```

만약 변경된 필드에 따라 SQL을 동적으로 만든다면:

```sql
-- title만 변경 시
update books set title=? where id=?

-- author만 변경 시
update books set author=? where id=?

-- 둘 다 변경 시
update books set author=?, title=? where id=?
```

엔티티 필드 수가 N개면, 조합에 따라 최대 2^N개의 서로 다른 SQL이 생길 수 있어요. DB 입장에서는 매번 새로운 모양의 SQL이라 실행 계획을 새로 짜야 하니 성능에 안 좋습니다.

**이유 2: 변경 메서드를 쓰는 이유 ≠ "SQL 최소화"**

여기서 중요한 포인트: `changeTitle()` 같은 메서드를 쓰는 목적은 **"SQL의 SET절을 줄이기 위해서"가 아니에요**. 진짜 목적은:

```java
public void changeTitle(String title){
    if (title == null || title.isBlank()) {
        throw new IllegalArgumentException("제목은 비어있을 수 없습니다");
    }
    this.title = title;
}
```

- **의도 명확화**: "이건 제목 변경 동작이다"
- **검증 로직 캡슐화**: 잘못된 값이 들어오는 걸 막음
- **무분별한 변경 차단**: `setAuthor()`도 만들어두면 아무나 작가 정보도 바꿀 수 있게 됨

즉, **"어떤 필드를, 어떤 조건에서, 어떻게 바꿀 수 있는가"를 객체 스스로 통제**하는 게 목적이고, SQL의 모양은 그 결과로 따라오는 부차적인 거예요.

**만약 정말 변경된 필드만 UPDATE하고 싶다면 - `@DynamicUpdate`**

```java
@Entity
@DynamicUpdate
@Table(name = "books")
public class Book {
    ...
}
```

이 어노테이션을 붙이면:

```sql
-- title만 변경했을 때
update books set title=? where id=?
```

실제로 변경된 필드만 SQL에 포함돼요.

**그럼 `@DynamicUpdate`는 항상 쓰면 좋은가?**

아니에요, 트레이드오프가 있어요:

|                                      | 기본 (정적 SQL)             | `@DynamicUpdate`                  |
| ------------------------------------ | --------------------------- | --------------------------------- |
| SQL 모양                             | 항상 동일 (캐싱 유리)       | 상황마다 다름 (캐싱 불리)         |
| 컬럼 수가 적은 엔티티                | 차이 거의 없음              | 큰 이득 없음                      |
| 컬럼 수가 매우 많은 엔티티 (수십 개) | 불필요한 컬럼까지 매번 전송 | 필요한 것만 전송 → 유리할 수 있음 |

대부분의 일반적인 엔티티(컬럼 5~10개 수준)에서는 기본 설정이 더 권장돼요. 컬럼이 아주 많고(예: 30개+) 그중 일부만 자주 바뀌는 특수한 경우에만 `@DynamicUpdate`를 고려합니다.

**정리**

| 질문                              | 답                                                                       |
| --------------------------------- | ------------------------------------------------------------------------ |
| `changeTitle()`을 쓰는 이유는?    | SQL을 줄이려는 게 아니라, **의도 명확화 + 검증 + 캡슐화**                |
| 왜 UPDATE에 모든 컬럼이 나오는가? | Hibernate가 **SQL을 미리 정적으로 만들어두고 캐싱**하기 때문 (기본 동작) |
| 변경된 필드만 UPDATE하려면?       | `@DynamicUpdate` (단, 컬럼이 많을 때만 효과적)                           |

**한 줄 요약**

"`changeTitle()`은 **'어떻게 바뀌는지'를 통제하는 메서드**고, SQL에 모든 컬럼이 나오는 건 **'얼마나 효율적으로 DB에 보내는지'에 대한 Hibernate의 별개 설정**이에요. 둘은 서로 다른 레이어의 문제입니다."

---

**`em.find()`의 핵심 용도: PK로 단건 엔티티를 영속성 컨텍스트에 올리기**

**1. 단건 조회 (가장 기본)**

```java
Item item = em.find(Item.class, 1L);
```

PK 값을 알고 있을 때, 그 엔티티 하나를 가져오는 가장 직접적인 방법.

**2. Update (변경 감지, Dirty Checking)**

맞아, **update할 때도 `find()`를 많이 써.** 오히려 JPA의 update 방식 자체가 `find()`를 전제로 함:

```java
@Transactional
public void updateItemName(Long itemId, String newName) {
    Item item = em.find(Item.class, itemId); // 1. 영속 상태로 조회
    item.setItemName(newName);                // 2. 필드 값만 변경 (setter or 변경 메서드)
    // em.update() 같은 메서드는 없음!
} // 3. 트랜잭션 커밋 시점에 변경 감지(Dirty Checking) → UPDATE 쿼리 자동 발생
```

**왜 이게 가능한가?**

- `find()`로 가져온 엔티티는 **영속성 컨텍스트**가 관리하는 상태가 됨
- 영속성 컨텍스트는 `find()` 시점의 엔티티 스냅샷을 저장해둠
- 트랜잭션 끝날 때 현재 엔티티 상태와 스냅샷을 비교해서 **다른 점이 있으면 자동으로 UPDATE SQL 생성**
- 그래서 `em.persist()`나 별도 update 메서드 호출 없이, **setter만 호출하면 끝**

**3. 연관관계 설정/변경**

```java
@Transactional
public void changeCategory(Long itemId, Long newCategoryId) {
    Item item = em.find(Item.class, itemId);
    Category category = em.find(Category.class, newCategoryId);
    item.setCategory(category); // FK 변경 → Dirty Checking으로 UPDATE
}
```

---

**`find()`를 안 쓰는/못 쓰는 경우**

| 상황                                                        | 사용 메서드                                 |
| ----------------------------------------------------------- | ------------------------------------------- |
| PK가 아닌 조건으로 조회 (이름, 카테고리 등)                 | JPQL, QueryDSL, Spring Data JPA 쿼리 메서드 |
| 여러 건 리스트 조회                                         | JPQL `getResultList()`, `findAll()`         |
| 단순 존재 확인만 필요                                       | `existsById()` 또는 `count` 쿼리            |
| 엔티티를 메모리에 올리지 않고 직접 UPDATE/DELETE SQL만 실행 | JPQL `executeUpdate()`, `@Modifying`        |

**참고: 벌크 업데이트는 find() 없이도 가능**

```java
@Modifying
@Query("update Item i set i.itemName = :name where i.id = :id")
void updateItemName(@Param("id") Long id, @Param("name") String name);
```

이건 `find()` 거치지 않고 SQL UPDATE를 직접 실행 — 다건 처리할 때 효율적이지만, **영속성 컨텍스트와 동기화 안 됨**(주의 필요).

---

**정리**

| 메서드                 | 특징                                                     |
| ---------------------- | -------------------------------------------------------- |
| `em.find(Class, PK)`   | PK로 영속 엔티티 조회 → setter로 update (Dirty Checking) |
| JPQL `getResultList()` | 조건 검색, 여러 건, 단방향/JOIN 조회                     |
| `@Modifying @Query`    | find 없이 SQL 직접 실행 (벌크)                           |

**한 줄 요약**: `find()`는 PK로 엔티티를 영속성 컨텍스트에 올려서 "변경 감지(Dirty Checking)"를 가능하게 하는 게 핵심이고, 단순 조회뿐 아니라 setter만으로 update를 하기 위한 표준 패턴이야.

---

## 1. detach, clear, close 차이

**비유: 영속성 컨텍스트 = "관리 명부가 있는 사무실"**

| 메서드           | 대상                 | 효과                                                                                | 비유                                                             |
| ---------------- | -------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `detach(entity)` | 특정 엔티티 1개      | 그 엔티티만 영속성 컨텍스트에서 빼냄                                                | 명부에서 "특정 직원 1명"만 퇴사 처리                             |
| `clear()`        | 영속성 컨텍스트 전체 | 모든 엔티티를 준영속 상태로 만들고, 컨텍스트를 비움 (EntityManager 자체는 살아있음) | 사무실은 그대로 두고, 명부에 적힌 직원 전체를 한꺼번에 퇴사 처리 |
| `close()`        | EntityManager 자체   | 영속성 컨텍스트 + EntityManager 자체를 완전히 종료                                  | 사무실을 통째로 폐업                                             |

**코드로 비교**

```java
Product product = em.find(Product.class, "p001"); // 영속 상태

// 1) detach - 이 엔티티만 분리
em.detach(product);
// product는 이제 "준영속" 상태. 다시 em.find()하면 새로 DB 조회함

// 2) clear - 전체 초기화
em.clear();
// 모든 영속 엔티티가 준영속됨. em은 계속 쓸 수 있음 (새로 persist/find 가능)

// 3) close - EntityManager 종료
em.close();
// 이후 em으로 아무것도 못 함 (사용 시 예외 발생)
```

**"준영속(Detached)" 상태란?**

영속 상태였다가 영속성 컨텍스트에서 분리된 상태예요. 엔티티 객체 자체는 메모리에 살아있지만, JPA가 더 이상 "관리"하지 않아요. 그래서:

```java
product.setName("새이름"); // 값은 바뀌지만
// → 변경 감지(Dirty Checking) 대상이 아니므로 DB에 반영 안 됨!
```

**실무에서 자주 쓰는 건 `clear()`**

대량의 데이터를 처리할 때, 영속성 컨텍스트에 엔티티가 계속 쌓이면 메모리 부담이 커져요(1차 캐시가 무한정 커짐). 그래서:

```java
for (int i = 0; i < 10000; i++) {
    em.persist(new Product(...));
    if (i % 100 == 0) {
        em.flush(); // 쌓인 SQL 전송
        em.clear(); // 영속성 컨텍스트 비우기 (메모리 확보)
    }
}
```

---

네, `merge()`는 실무에서 자주 쓰이지만 동작 원리를 정확히 모르면 헷갈리는 메서드예요.

**`merge()`가 필요한 상황**

```java
// 1. 트랜잭션 1: 엔티티 조회 후 종료
@Transactional
public Product getProduct(String id) {
    return em.find(Product.class, id); // 영속 상태로 반환
}
// 트랜잭션 끝남 → 반환된 product는 "준영속(Detached)" 상태가 됨

// 2. 클라이언트(또는 다른 계층)에서 값 수정
Product product = getProduct("p001");
product.setName("수정된 이름"); // ⚠️ 준영속 상태라 변경 감지 안 됨!

// 3. 트랜잭션 2: 이 변경을 DB에 반영하고 싶음
@Transactional
public void update(Product product) {
    // 이대로는 변경 감지가 안 일어나서 UPDATE가 안 나감
}
```

준영속 엔티티는 JPA가 더 이상 관리하지 않기 때문에, 아무리 필드 값을 바꿔도 자동으로 DB에 반영되지 않아요. 이때 `merge()`가 필요합니다.

**`merge()`의 동작 원리**

```java
@Transactional
public Product update(Product detachedProduct) {
    Product managed = em.merge(detachedProduct);
    return managed;
}
```

내부적으로 일어나는 일:

```
1. detachedProduct의 PK("p001")를 확인
2. 1차 캐시에 "p001"이 있는지 확인
   → 없으면 DB에서 조회해서 1차 캐시에 새로운 영속 엔티티 생성
3. 그 영속 엔티티(managed)의 필드값을, detachedProduct의 값으로 "전부 복사(덮어쓰기)"
4. managed 엔티티를 반환 (이게 진짜 영속 상태)
5. commit 시점에 managed 엔티티 기준으로 변경 감지 → UPDATE
```

**핵심: `merge()`는 "새로운 영속 객체를 만들어서 반환"**

```java
Product detached = ...; // 준영속 (트랜잭션 밖에서 받아온 객체)
Product managed = em.merge(detached);

System.out.println(detached == managed); // false!
// detached는 여전히 준영속 상태로 그대로 남아있음
// managed가 새로 영속화된 진짜 엔티티
```

**비유로 정리: "복사해서 새 명부에 등록"**

준영속 객체(detached)는 "회사를 그만둔 직원이 들고 있는 자기 인사기록 복사본"이에요. 이 사람이 "제 정보 좀 업데이트해주세요"라고 회사에 새 정보를 들고 오면:

```
1. 회사(영속성 컨텍스트)는 "이 사람 사번이 p001이네" 확인
2. 인사 명부(1차 캐시)에서 p001을 찾음 (없으면 DB에서 새로 조회)
3. 명부에 있는 진짜 기록(managed)의 내용을, 이 사람이 들고 온 정보(detached)로 덮어씀
4. 명부의 진짜 기록(managed)을 돌려줌
   ※ 이 사람이 원래 들고 있던 복사본(detached) 자체는 여전히 "퇴사자 신분"
```

**전체 비교표**

| 메서드            | 입력 상태         | 출력 상태                          | 동작                                                               |
| ----------------- | ----------------- | ---------------------------------- | ------------------------------------------------------------------ |
| `persist(entity)` | 비영속(Transient) | 영속(Persistent)                   | 새 엔티티를 영속성 컨텍스트에 신규 등록                            |
| `merge(entity)`   | 준영속(Detached)  | 영속(Persistent) - **새로운 객체** | PK로 기존 영속 엔티티를 찾아오거나 새로 만들고, 값을 복사해서 반환 |
| `detach(entity)`  | 영속              | 준영속                             | 영속성 컨텍스트에서 분리                                           |
| `find(...)`       | -                 | 영속                               | 1차 캐시 → 없으면 DB 조회                                          |

**주의: `merge()`의 위험성 - "전체 덮어쓰기"**

```java
// detached 객체의 일부 필드가 null이라면?
Product detached = new Product();
detached.setId("p001");
detached.setName("새 이름");
// detached.price는 설정 안 함 → 기본값 0 또는 null

Product managed = em.merge(detached);
// managed.price도 0(또는 null)으로 덮어써짐!!
// 원래 DB에 price=50000이었더라도 사라짐 😱
```

`merge()`는 "넘겨준 객체의 **모든 필드값으로 덮어쓰기**" 때문에, 일부 필드만 빼고 넘기면 그 필드들이 의도치 않게 초기화될 수 있어요. 그래서 실무에서는 `merge()`를 직접 쓰기보다, `find()`로 영속 엔티티를 가져온 뒤 **변경 메서드를 호출**(예: `product.changePrice(...)`)해서 변경 감지(Dirty Checking)로 처리하는 패턴을 더 권장해요.

```java
// 권장 패턴
@Transactional
public void updatePrice(String id, int newPrice) {
    Product product = em.find(Product.class, id); // 영속 상태로 가져옴
    product.changePrice(newPrice); // 일부 필드만 안전하게 변경
    // commit 시점에 변경 감지로 자동 UPDATE
}
```

**한 줄 정리**

`merge()`는 "준영속 객체의 데이터를 가지고, **새로운 영속 엔티티를 만들어서 그 위에 통째로 복사해 붙이는** 작업"이에요. 마치 졸업생(준영속)이 들고 온 자기소개서를 학교(영속성 컨텍스트)가 받아서, 학교 보관용 새 서류(영속 엔티티)에 그대로 베껴 쓰는 것과 비슷합니다.

---

# 엔티티 `@Setter` 지양하는 이유

**1. 객체의 일관성(불변성) 깨짐**

```java
// @Setter가 있으면 이런 코드가 가능해짐
product.setPrice(-1000); // 가격이 음수?
product.setName(null); // 이름이 null?
```

setter는 "아무 값이나 마음대로 바꿀 수 있는 문"을 열어두는 거예요. 비즈니스 규칙(예: "가격은 0보다 커야 한다")을 검증할 틈 없이 외부에서 값을 직접 바꿀 수 있게 되죠.

**2. 의도가 불명확해짐**

```java
// setter 방식 - "뭘 하는 건지" 코드만 봐서는 모름
product.setStock(product.getStock() - quantity);
product.setStatus("SOLD_OUT");

// 의미 있는 메서드 방식 - 의도가 명확함
product.decreaseStock(quantity);
```

`setStock()`만 보면 "재고를 줄이는 중인지, 재고를 초기화하는 중인지, 재입고하는 중인지" 알 수 없어요. 반면 `decreaseStock()`, `restock()`처럼 **비즈니스 의미를 담은 메서드**는 코드 자체가 문서가 됩니다.

**3. 변경 추적이 어려워짐**

```java
// 어디서 바뀌었는지 추적하기 힘듦
product.setName("새 이름"); // 서비스 A에서 호출
product.setName("또 다른 이름"); // 서비스 B에서 호출
```

setter가 모든 필드에 열려있으면, 코드베이스 전체에서 어디서 이 값이 바뀌는지 찾기 어려워요. 디버깅할 때 "도대체 누가 이 값을 바꿨지?"를 추적하기 힘들죠.

**4. JPA의 변경 감지(Dirty Checking)와 얽힘**

```java
@Transactional
public void updatePrice(Long id, int newPrice) {
    Product product = em.find(Product.class, id);
    product.setPrice(newPrice); // 이것만으로 UPDATE가 나감 (커밋 시점에)
}
```

JPA 영속성 컨텍스트는 엔티티의 필드 변화를 감지해서 자동으로 UPDATE를 날려요. setter로 아무 필드나 바꿀 수 있으면, 어떤 검증도 거치지 않고 DB에 잘못된 값이 그대로 반영될 수 있어요.

**대안: 의미 있는 변경 메서드**

```java
@Entity
@Getter // 조회는 자유롭게
@NoArgsConstructor(access = AccessLevel.PROTECTED) // JPA 기본 생성자만 허용
public class Product {
    @Id
    private String id;
    private String name;
    private int price;
    private int stock;

    @Builder
    public Product(String id, String name, int price, int stock) {
        this.id = id;
        this.name = name;
        this.price = price;
        this.stock = stock;
    }

    // setter 대신, 검증 로직을 포함한 의미 있는 메서드
    public void changePrice(int newPrice) {
        if (newPrice < 0) {
            throw new IllegalArgumentException("가격은 0 이상이어야 합니다.");
        }
        this.price = newPrice;
    }

    public void decreaseStock(int quantity) {
        if (this.stock < quantity) {
            throw new IllegalStateException("재고가 부족합니다.");
        }
        this.stock -= quantity;
    }
}
```

**한 줄 정리**

`@Setter`는 "누구나 아무 때나 아무 값으로 바꿀 수 있는 만능 리모컨"을 주는 거고, 의미 있는 메서드는 "이 동작은 이 조건일 때만, 이런 검증을 거쳐서만 가능해"라고 **객체 스스로 자신의 상태를 보호**하게 만드는 거예요. 이게 객체지향에서 말하는 "캡슐화(encapsulation)"의 핵심이기도 하죠.

---

# jpa 기본생성자 + protected 쓰는 이유

@NoArgsConstructor(access = AccessLevel.PROTECTED)
JPA가 기본 생성자(no-args constructor)를 요구하는 이유와, 그걸 `protected`로 설정하는 이유는 서로 다른 문제를 해결해요.

**1. 왜 기본 생성자가 필요한가? (JPA의 내부 동작 방식)**

JPA(Hibernate)는 DB에서 데이터를 조회한 후, 그 결과를 엔티티 객체로 만들어줘요. 이때 **리플렉션(Reflection)**을 사용합니다.

```java
// JPA 내부적으로 이런 식으로 동작 (개념적으로)
Product product = Product.class.getDeclaredConstructor().newInstance(); // 빈 객체 생성
product.setName(reflectionResult.get("name")); // 리플렉션으로 필드에 값 채워넣기
product.setPrice(reflectionResult.get("price"));
```

**비유로 설명하면**

JPA는 "빈 그릇(빈 객체)을 먼저 만들고, 그 안에 DB에서 가져온 재료(데이터)를 하나씩 채워 넣는" 방식으로 동작해요. 그릇을 만들려면 "빈 그릇을 만드는 방법(기본 생성자)"이 있어야 하는 거죠.

만약 다음과 같은 생성자만 있다면:

```java
public Product(String id, String name, int price) { ... } // 매개변수 있는 생성자만 존재
```

JPA는 "빈 그릇"을 만들 방법이 없어서 에러가 나요.

**2. 왜 `protected`로 설정하는가? (개발자 vs JPA의 접근 권한 차이)**

```java
@NoArgsConstructor(access = AccessLevel.PROTECTED)
public class Product { ... }
```

이 부분은 "**JPA는 쓸 수 있게 하되, 일반 개발자는 못 쓰게 막는다**"는 의도예요.

| 접근 제어자 | 같은 패키지/프록시에서 접근                       | 외부 개발자가 직접 호출 |
| ----------- | ------------------------------------------------- | ----------------------- |
| `public`    | 가능                                              | 가능 ❌ (문제)          |
| `protected` | 가능 (Hibernate 프록시는 상속 기반이라 접근 가능) | 불가능 ✅               |
| `private`   | 불가능 → JPA 동작 안 함 ❌                        | 불가능                  |

**`public`이면 왜 문제인가**

```java
Product product = new Product(); // 빈 객체가 생성됨
// id도 null, name도 null, price도 0...
// 이게 의미 있는 상태인가? 아니죠!
```

기본 생성자가 `public`이면, 개발자가 의도치 않게 "필드가 전부 비어있는, 의미 없는 엔티티 객체"를 만들 수 있게 돼요. 이건 객체의 일관성을 깨는 행위죠.

**왜 `private`이면 안 되는가**

Hibernate는 지연 로딩(Lazy Loading) 시 **프록시(Proxy) 객체**를 만들어요. 이 프록시는 엔티티를 **상속**받은 자식 클래스 형태로 동작하는데, 자식 클래스가 부모의 생성자를 호출하려면 최소 `protected`는 되어야 해요. `private`이면 자식 클래스에서 접근이 안 되니까 프록시 생성이 실패합니다.

**정리**

| 질문                   | 답                                                                                                                   |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 기본 생성자가 왜 필요? | JPA가 리플렉션으로 "빈 객체"를 만든 뒤 필드를 채우는 방식으로 동작하기 때문                                          |
| 왜 `protected`?        | JPA(프록시 포함)는 접근 가능하게 열어주면서, 일반 개발자가 `new Product()`로 의미 없는 빈 객체를 만드는 걸 막기 위해 |

**한 줄 요약**

"JPA야, 너만 쓸 수 있는 뒷문(protected 기본 생성자)을 줄게. 근데 일반 출입구(public 생성자)로는 꼭 필요한 값들을 다 채워서 들어와야 해(`@Builder` 또는 `@AllArgsConstructor`)."

