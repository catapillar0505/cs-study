# JPA N+1 문제

## N+1 문제란?

N+1 문제는 JPA에서 연관관계가 있는 엔티티를 조회할 때 발생하는 **성능 문제**입니다.

1개의 쿼리(N을 조회)를 실행했는데, 추가로 N개의 쿼리가 더 실행되는 현상입니다.  
결국 총 **N+1번의 쿼리**가 나가게 됩니다.

---

## 구체적인 예시

### 엔티티 구조

```
Team (1) ──────────< Member (N)
```

```java
@Entity
public class Team {
    @Id @GeneratedValue
    private Long id;
    private String name;

    @OneToMany(mappedBy = "team", fetch = FetchType.LAZY)
    private List<Member> members = new ArrayList<>();
}

@Entity
public class Member {
    @Id @GeneratedValue
    private Long id;
    private String name;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "team_id")
    private Team team;
}
```

---

### 문제 발생 시나리오

DB에 Team이 5개, 각 Team마다 Member가 여러 명 있다고 가정합니다.

```java
List<Team> teams = teamRepository.findAll(); // 쿼리 1번 실행

for (Team team : teams) {
    System.out.println(team.getMembers()); // 각 팀마다 쿼리 1번씩 실행
}
```

**실제로 실행되는 SQL:**

```sql
-- 1번: 전체 Team 조회
SELECT * FROM team;

-- 2번: team_id = 1 인 Member 조회
SELECT * FROM member WHERE team_id = 1;

-- 3번: team_id = 2 인 Member 조회
SELECT * FROM member WHERE team_id = 2;

-- 4번: team_id = 3 인 Member 조회
SELECT * FROM member WHERE team_id = 3;

-- 5번: team_id = 4 인 Member 조회
SELECT * FROM member WHERE team_id = 4;

-- 6번: team_id = 5 인 Member 조회
SELECT * FROM member WHERE team_id = 5;
```

Team이 5개이므로 총 **1 + 5 = 6번** 쿼리가 실행됩니다.

> Team이 1000개라면? → **1001번** 쿼리가 실행됩니다. 🚨

---

## 왜 발생하는가?

### LAZY 로딩(지연 로딩) 때문

- `FetchType.LAZY`: 연관 엔티티를 **실제로 사용하는 시점**에 쿼리를 날림
- `findAll()`로 Team 목록만 가져올 때는 Member를 조회하지 않음
- 루프를 돌면서 `team.getMembers()`를 호출하는 순간 각각 Member 쿼리가 실행됨

### EAGER 로딩(즉시 로딩)은 해결책이 아님

`FetchType.EAGER`로 바꿔도 N+1 문제는 여전히 발생합니다.  
JPQL로 `findAll()`을 실행하면, JPA는 JPQL 결과를 먼저 가져온 후  
EAGER 설정에 따라 각 Team마다 Member를 **개별 쿼리**로 추가 조회하기 때문입니다.

---

## 해결 방법

### 1. Fetch Join (가장 일반적)

JPQL에서 `JOIN FETCH`를 사용해 **한 번의 쿼리**로 Team과 Member를 함께 조회합니다.
기본 join fetch는 inner join 맞아. left join fetch라고 명시해야 left outer join이 됨.

```java
// Repository
@Query("SELECT t FROM Team t JOIN FETCH t.members")
List<Team> findAllWithMembers();
```

**실행 SQL:**

```sql
SELECT t.*, m.*
FROM team t
INNER JOIN member m ON t.id = m.team_id;
```

쿼리가 **1번**만 실행됩니다. ✅

---

### 2. @EntityGraph

`@Query` 없이도 Spring Data JPA의 `@EntityGraph` 어노테이션으로 Fetch Join을 적용할 수 있습니다.

```java
// Repository
@EntityGraph(attributePaths = {"members"})
List<Team> findAll();
```

---

### 3. Batch Size 설정

Fetch Join이 어려운 상황(페이징 등)에서 사용합니다.  
IN 절을 사용해 여러 Team의 Member를 **한 번에** 조회합니다.

```yaml
# application.yml
spring:
  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 100
```

또는 특정 필드에만 적용:

```java
@BatchSize(size = 100)
@OneToMany(mappedBy = "team")
private List<Member> members = new ArrayList<>();
```

**실행 SQL (batch size = 100, Team이 5개일 때):**

```sql
-- 1번: 전체 Team 조회
SELECT * FROM team;

-- 2번: IN 절로 한꺼번에 Member 조회
SELECT * FROM member WHERE team_id IN (1, 2, 3, 4, 5);
```

총 **2번**의 쿼리로 줄어듭니다. ✅

---

## 해결 방법 비교

| 방법 | 쿼리 횟수 | 페이징 가능 | 비고 |
|------|-----------|-------------|------|
| 기본 (N+1 발생) | 1 + N | ✅ | 성능 문제 |
| Fetch Join | 1 | ❌ (컬렉션의 경우) | 가장 직관적 |
| @EntityGraph | 1 | ❌ (컬렉션의 경우) | Fetch Join과 동일 |
| Batch Size | 2 이상 | ✅ | 페이징 시 권장 |

---

## 핵심 정리

```
N+1 문제 = "1번 조회해도 N번 추가 쿼리가 실행되는 현상"

원인: LAZY 로딩 + 반복문에서 연관 엔티티 접근
해결: Fetch Join 또는 Batch Size 설정
```

> **실무 팁**: 페이징이 필요 없으면 **Fetch Join**, 페이징이 필요하면 **Batch Size**를 사용하세요.

좋은 질문! 헷갈리기 쉬운 부분이야.

---

## 핵심 개념 분리

**더티 체킹** = 엔티티 **필드값 변화** 감지 → DB UPDATE  
**@EntityListeners** = 엔티티 **생명주기 이벤트** 감지 → 콜백 실행

완전히 다른 역할이야.

---

## 더티 체킹이 하는 일

```java
User user = em.find(User.class, 1L);
user.setName("김지나");  // 변경
// 트랜잭션 커밋 시 → UPDATE users SET name='김지나' WHERE id=1
```

더티 체킹은 **"뭐가 바뀌었는지"** 만 알아.  
**"언제 바뀌었는지"**, **"누가 만들었는지"** 는 모르지.

---

## @EntityListeners가 하는 일

```java
@EntityListeners(AuditingEntityListener.class)
@Entity
public class Post {

    @CreatedDate
    private LocalDateTime createdAt;   // 최초 저장 시점 자동 입력

    @LastModifiedDate
    private LocalDateTime updatedAt;   // 수정될 때마다 자동 갱신

    @CreatedBy
    private String createdBy;          // 누가 만들었는지

    @LastModifiedBy
    private String modifiedBy;         // 누가 수정했는지
}
```

더티 체킹은 저 4개 필드를 **알아서 채워주지 않아.**  
네가 매번 직접 `post.setCreatedAt(LocalDateTime.now())` 해줘야 함.

`@EntityListeners`는 그걸 **자동화**해주는 것.

---

## 생명주기 이벤트 종류

| 어노테이션 | 발생 시점 |
|---|---|
| `@PrePersist` | `persist()` 직전 |
| `@PostPersist` | `persist()` 직후 |
| `@PreUpdate` | UPDATE 직전 |
| `@PostUpdate` | UPDATE 직후 |
| `@PreRemove` | `remove()` 직전 |
| `@PostRemove` | `remove()` 직후 |

---

## 한 줄 요약

더티 체킹은 **"값 변화 → DB 반영"**, `@EntityListeners`는 **"DB 이벤트 → 콜백 실행"** — 감지 대상 자체가 달라 🎯

# 새롭게 알게된 것


- 아이덴티티 생성 전략일 때 persist시 insert만 즉시 날라가고, delete update 쿼리 등은 모두 쌓임
- @id는 단독으로 쓰일 수 있지만, @generatedValue는 pk 생성전략이므로 단독으로 못쓰이고 @id랑 함께랑만 쓰일 수 있다. 만약 단독으로 쓰면 컴파일 오류남.

## redis vs 2차 캐시

- 분산 환경(서버 여러 대)에서는 2차 캐시가 각 서버 JVM 메모리에 따로 존재해서, 한 서버에서 데이터가 변경돼도 다른 서버의 2차 캐시는 갱신되지 않는 **캐시 불일치 문제**가 생깁니다. 이를 해결하려면 Redis 같은 분산 캐시(Ehcache + Redis, Hazelcast 등) 연동이 필요해서 설정이 꽤 복잡해집니다.
- 실무에서는 보통 2차 캐시 대신 **Redis를 애플리케이션 레벨 캐시**로 직접 사용합니다 (`@Cacheable`(Spring Cache) + Redis, 또는 직접 RedisTemplate 사용). 이 방식이 더 명시적이고 컨트롤하기 쉽습니다.
- 2차 캐시는 주로 **읽기 전용에 가깝고 거의 안 바뀌는 데이터** (예: 코드 테이블, 카테고리, 공통 코드 등) 한정으로만 쓰는 경우가 많습니다.

## 실무 우선순위

| 상황 | 선택 |
|---|---|
| 거의 안 바뀌는 참조 데이터, 단일 서버 | 2차 캐시 (`@Cache`) 고려 가능 |
| 분산 서버 환경, 자주 갱신되는 데이터 | Redis + `@Cacheable` |
| 일반적인 실무 | **Redis 기반 캐싱이 압도적으로 많음** |

한 줄 요약: **`@Cache`(2차 캐시)는 분산 환경에서 불일치 문제 때문에 실무에서는 잘 안 쓰고, 대부분 Redis 캐싱으로 대체한다.**

**Spring의 `@Cacheable`은 JPA 2차 캐시와 완전히 별개의 메커니즘**입니다.

| 구분 | JPA 2차 캐시 (`@Cache`) | Spring `@Cacheable` |
|---|---|---|
| 소속 | JPA/Hibernate 영역 | Spring AOP 영역 |
| 동작 방식 | EntityManagerFactory 레벨에서 엔티티를 캐싱 | 메서드 호출 결과(반환값)를 캐싱 |
| 캐시 저장소 | Ehcache, Infinispan 등 (JPA provider 종속) | Redis, Caffeine, ConcurrentMap 등 직접 선택 |
| 적용 대상 | 엔티티 객체 | 메서드 (어떤 메서드든) |
| 동작 원리 | `em.find()` 호출 시 내부적으로 자동 개입 | AOP 프록시가 메서드 호출을 가로채서 캐시 확인 |

```java
@Cacheable(value = "members", key = "#id")
public Member getMember(Long id) {
    return memberRepository.findById(id).orElseThrow();
}
````

이 코드는 JPA의 1차/2차 캐시와 무관하게, **Spring AOP가 메서드 자체의 결과를 Redis 같은 캐시 저장소에 저장**하는 방식입니다. JPA 2차 캐시를 켜지 않아도 동작합니다.

한 줄 요약: **2차 캐시는 JPA 영속성 레이어의 엔티티 캐싱이고, `@Cacheable`은 Spring AOP가 메서드 결과를 캐싱하는 것 — 서로 다른 차원의 캐싱이다.**

```

실무에서는 코드 테이블/카테고리/공통 코드용으로 **2차 캐시보다 Redis나 로컬 캐시(Caffeine)를 더 많이 씁니다.** 다만 "아예 안 쓰는" 선택지는 거의 없습니다 — 이런 데이터는 거의 안 바뀌면서 조회는 매우 빈번하기 때문에 캐싱 안 하면 DB에 불필요한 부하가 계속 갑니다.

## 실무에서 더 흔한 선택: Caffeine (로컬 캐시) > Redis > 2차 캐시

| 방식 | 적합한 경우 | 단점 |
|---|---|---|
| **Caffeine (로컬 메모리)** | 단일 서버 또는 서버별로 약간의 불일치 허용 가능 | 분산 환경에서 서버 간 불일치 (보통 TTL 짧게 줘서 해결) |
| **Redis** | 분산 환경, 서버 간 일관성 중요 | 네트워크 호출 비용(그래도 DB보다 훨씬 빠름) |
| **JPA 2차 캐시** | 거의 안 씀 | 설정 복잡, 분산 환경 불일치 문제 동일하게 있음, Redis 대비 이점 없음 |
| **캐시 없음** | 데이터가 정말 작고 트래픽도 적을 때만 | 거의 선택 안 함 |

## 왜 코드 테이블에 2차 캐시 대신 Caffeine/Redis를 더 쓰는가

코드 테이블 같은 데이터는 애플리케이션 시작 시 통째로 메모리에 올려두고 거의 안 갈아엎는 패턴이 많습니다. 굳이 JPA 엔티티 캐싱 메커니즘(설정 복잡, provider 종속)을 쓸 필요 없이, 단순 `Map<String, List<Code>>` 형태로 애플리케이션 시작 시 로딩하거나 Caffeine으로 캐싱하는 게 더 가볍고 명확합니다.

```java
@Cacheable(value = "commonCodes", key = "#groupCode")
public List<CommonCode> getCommonCodes(String groupCode) {
    return commonCodeRepository.findByGroupCode(groupCode);
}
```

한 줄 요약: **공통 코드/카테고리는 캐싱 자체는 필수지만, 실무에서는 2차 캐시 대신 Caffeine(단일 서버) 또는 Redis(분산 환경)를 더 많이 쓴다.**

# flush와 commit의 차이

- **flush**: 쓰기 지연 저장소에 쌓인 SQL을 DB로 전송해서 **실행**시킴. DB 입장에서는 이미 쿼리를 받아서 처리한 상태 (예: update면 해당 row가 실제로 변경됨). 하지만 이건 **현재 트랜잭션 내부**에서 일어난 변경이라, 아직 **커밋되지 않은 상태**임.
- **commit**: 그 트랜잭션에서 일어난 변경들을 **확정**시켜서, 다른 트랜잭션(다른 커넥션)에서도 그 결과를 볼 수 있게 함. 또는 DB 장애 시에도 영구적으로 남도록 함 (durability).

그러니까:

```
flush  → "야 DB야, 이 UPDATE문 실행해" → DB가 실행은 했음 (트랜잭션 내부에 임시 반영)
commit → "이제 이거 확정해, 다른 사람들도 볼 수 있게" → 영구 반영 + 외부에 공개
```

flush만 하고 commit을 안 하면 (혹은 rollback하면) 그 변경은 **DB에 실행은 됐지만 사라짐**. 즉 flush = 반영의 일종이지만 "확정된 반영"은 아니고, commit이 돼야 진짜 영구적으로 반영됨.

그래서 더 정확한 표현은:

- flush = SQL을 DB에 **실행 요청 + 실행** (트랜잭션 범위 내에서)
- commit = 그 실행 결과를 **영구 확정** (durability 보장 + 다른 트랜잭션에 공개)

한 줄 요약: flush는 "쿼리 실행", commit은 "그 실행 결과의 영구 확정"이고, flush 없이 commit만 있는 게 아니라 **commit 직전에 flush가 선행**되는 구조야.