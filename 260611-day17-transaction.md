# 트랜잭션

> 트랜잭션은 여러 SQL을 하나의 논리적 단위로 묶어, 전부 성공하거나 전부 실패하도록 원자성을 보장하는 DB의 실행 단위입니다

> 서비스 로직에 구현함

# 트랜잭션의 종류

```
(1) 수동
= 프로그램 방식 트랝잭션
개발자가 직접 트랜잭션 처리 (begin, commit, rollback)
```

```
(2) 자동
= 선언적 트랜잭션
스프링 AOP 프록시 기술로 스프링이 처리(@Transactional)
실제 객체를 감싸는 프록시 객체를 만들어서, 프록시 객체가 메서드 호출을 가로채는 방식
```

> @Transactional은 프록시가 가로채야 작동 → 내부 호출(직접 트랜잭션 호출하면) -> 프록시가 가로채지 못함 → 트랜잭션 실패

> 수동 처리는 코드에 직접 박혀있음 → 누가 호출하든 항상 작동

# 왜 커스텀 예외는 런타임 예외를 상속받는가?

```
일반 예외는 컴파일 시점에 예외처리를 함
근데 실행 예외는 런타임 시점에 예외처리를 함
커스텀 예외는 비즈니스 규칙을 위반했을 때 개발자가 발생시키는 것이므로,
개발자가 일일이 예외 처리하여 컴파일 시점에 처리되는 것이 아니라, 런타임 시점에 전역 핸들러에 의해 한번에 처리되기 때문에 런타임 예외를 상속받아야 한다.
```

```
Unchecked(실행 예외, 런타임 예외) → 예외처리 안해도 컴파일러가 문제 안삼는 예외
그러므로 런타임 예외를 상속받아
호출부 마다 예외처리 코드를 적는 것을 방지하여,
전역 핸들러(@RestControllerAdvice)가 한 곳에서 처리
```

```
Checked(일반 예외) → 호출부마다 예외처리 안하면 컴파일러가 문제 삼는 예외

```

# 조회는 클래스 레벨, 나머지는 메서드 레벨에서 거는 이유

Service 클래스는 보통 조회가 쓰기보다 훨씬 많기 때문에,
그래서 기본값을 readOnly = true로 깔고, 쓰기 메서드만 예외 처리하는 게 더 효율적
반대로 하면 (readOnly = false 기본, 조회마다 readOnly = true) 붙여야 할 어노테이션이 훨씬 많음

클래스 레벨은 **"별말 없으면 다 이걸로 써"** 라는 디폴트 선언
메서드 레벨은 특별한 경우이므로 클래스 레벨과 다르게 지정하겠다는 의미

`@Transactional`의 `readOnly` 기본값은 `false`예요.
그래서 메서드에 `@Transactional`만 붙이면 자동으로 `readOnly = false`로 오버라이드

> 가장 많은 케이스를 클래스 레벨 디폴트로 깔고,
> 예외 케이스만 메서드 레벨에서 오버라이드하는 것 — 이게 이 패턴의 핵심이에요.

# DB 리플리케이션 라우팅 (?)

readOnly = true → 읽기 전용 복제 DB (Replica)
readOnly = false → 메인 DB (Primary)

# 내부 호출하면 프록시가 가로채지 못하는 이유

### 내부 호출은 왜 안 되냐면

```java
@Service
public class OrderService {

    public void order() {
        this.pay(); // this = 실제 객체, 프록시 아님!
    }

    @Transactional
    public void pay() { ... }
}
```

`this`는 프록시가 아니라 **실제 OrderService 객체 자신**이에요.

프록시를 완전히 건너뛰고 직접 호출하는 거라서 트랜잭션이 안 걸려요.

```
외부 → 프록시.order() → 실제객체.order() → 실제객체.pay()
                                              ↑
                                         프록시 없음!
```

### 프록시가 가로채는 조건

```java
paymentService.orderService.createOrder() 호출
        ↓
   프록시가 받음
        ↓
   "어, @Transactional 붙어있네 → 트랜잭션 시작!"
        ↓
   진짜 OrderService.createOrder() 실행
        ↓
   "끝났네 → 트랜잭션 커밋/롤백!"
```

> 외부에서 프록시 객체를 통해 호출할 때만

### Spring이 빈을 등록하는 방식

Spring은 `@Service` 등록할 때 실제 객체를 바로 주는 게 아니라, **프록시로 감싸서** 줘요.

```
Spring 컨테이너
└── OrderService 빈 = 프록시(실제 OrderService를 감싼)
```

그래서 다른 클래스에서 주입받으면 프록시를 받는 거예요.

```java
@Service
public class PaymentService {
    private final OrderService orderService; // 사실 프록시!

    public void pay() {
        orderService.order(); // 프록시 통과 → 트랜잭션 걸림 ✅
    }
}
```

### 한 줄 요약

프록시는 **"입구"** 예요. 외부에서 들어올 때만 가로챌 수 있고, 건물 안에서 이동하는 건 못 막아요.

## 쓰기/읽기 왜 구분해?

안전성 — 읽기 메서드에서 실수로 쓰기 못 하게 DB가 막아줌
효율성 — 읽기는 더티체킹, 락 등 쓰기에 필요한 리소스를 안 써도 되니까 가볍게 처리
