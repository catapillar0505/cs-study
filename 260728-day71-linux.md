# 리눅스 학습 정리 (Step by Step)

> 개념 의존성을 고려해, "먼저 알아야 이해되는 것"부터 순서대로 배치했습니다.
> 하드웨어·부팅 → 커널·프로세스 → 파일시스템 → 권한 → 사용자 → 명령어 → 네트워크 순서입니다.

---

## STEP 1. 운영체제의 큰 그림 — 유닉스, 리눅스, POSIX

### 유닉스(Unix) vs 리눅스(Linux)
- **유닉스**: 1970년대 벨 연구소에서 만든 원조 OS. 파생된 상용 OS들(Solaris, AIX, HP-UX, macOS의 기반 BSD 등)을 통칭.
- **리눅스**: 리누스 토르발스가 1991년에 유닉스를 "닮게(Unix-like)" 새로 만든 커널. 유닉스 코드를 가져온 게 아니라 철학·인터페이스(POSIX)를 따라 독립 개발.

| 구분 | 유닉스 | 리눅스 |
|---|---|---|
| 라이선스 | 대부분 상용/폐쇄형 | 오픈소스(GPL) |
| 소유 | 특정 회사 | 커널 소스 공개 |
| 예시 | Solaris, AIX, macOS(BSD) | Ubuntu, RHEL 등 |

### POSIX (Portable Operating System Interface)
- 유닉스 계열 OS들이 **공통으로 지켜야 할 표준(규약)**.
- "최소한 이런 함수·명령어·시스템 구조는 이런 이름/방식으로 제공해야 한다"를 규정.
- 규정 범위: 시스템 콜(`fork()`, `wait()`), 명령어(`ls`, `grep`), 쉘 문법, 권한 체계(rwx), 프로세스 개념 등.
- 콘센트 규격 표준에 비유 — 표준을 따르면 서로 호환됨.
- **"문법"보다는 "표준/규약"이 더 정확한 표현.**

### 리눅스 커널 vs 배포판
- **리눅스 = 커널 하나만**을 가리키는 말. 커널은 하드웨어를 제어하고 프로세스/메모리를 관리하는 핵심 부품.
- **배포판(Distribution)** = 커널 + 셸 + 패키지 관리자 + 유틸리티 등을 묶어 "설치해서 쓸 수 있는 완성 OS"로 만든 것.
- 비유: 커널 = 자동차 엔진, 배포판 = 엔진을 넣어 완성한 자동차 브랜드.

**대표 배포판 3계열**

| 계열 | 대표 배포판 | 패키지 관리자 |
|---|---|---|
| Debian 계열 | Ubuntu, Debian | `apt` |
| RHEL 계열 | RHEL, CentOS, Rocky Linux, Fedora | `dnf`/`yum` |
| Arch 계열 | Arch Linux, Manjaro | `pacman` |

- **RHEL** = Red Hat Enterprise Linux(상용). 읽는 법: "알에이치이엘"(가장 무난), "레드햇"(캐주얼).
- **RHEL vs Rocky Linux**: 거의 같은 코드베이스. RHEL은 상용(유료 지원), Rocky는 RHEL 소스를 재빌드한 무료 클론. CentOS의 성격 변화(CentOS Stream 전환) 이후 그 빈자리를 메우려 등장.

---

## STEP 2. 컴퓨터가 켜지는 과정 — 부팅

### 부팅 순서
```
1. 전원 켜짐
   ↓
2. BIOS / UEFI (마더보드 내장 펌웨어)
   - 하드웨어(CPU, 메모리) 점검(POST)
   - 부팅할 저장장치 결정 → 부트로더 실행
   ↓
3. 부트로더(GRUB)
   - OS 선택 메뉴 제공
   - 커널 파일을 디스크→메모리로 로드
   - 커널에 실행 권한 넘김
   ↓
4. 리눅스 커널
   - 하드웨어 초기화, 드라이버 로드
   - 최초의 사용자 공간 프로세스(systemd) 생성 + 실행
```

### 약자와 역할
- **BIOS** (Basic Input Output System): 오래된 펌웨어. 하드웨어 점검, 부트로더 실행.
- **UEFI** (Unified Extensible Firmware Interface): BIOS의 후속/개선판. 그래픽 UI, 2TB 이상 디스크 지원, 빠른 부팅, Secure Boot.
- **GRUB** (GRand Unified Bootloader): 부트로더. OS 선택 메뉴, 커널 로드.

### 커널이란?
- **물리적 장치가 아니라 소프트웨어(프로그램)**. 디스크(`/boot/vmlinuz-...`)에 파일로 저장되어 있다가 부팅 시 메모리로 로드되어 실행됨.
- "커널이 올라온다" = 디스크에 있던 커널을 메모리로 끌어올려 실행 시작.
- 역할: CPU 스케줄링, 메모리 관리, 하드웨어 드라이버 처리, 프로세스 간 통신, 파일시스템 관리.

### vmlinuz 이름의 유래
```
vmlinuz = vm(virtual memory, 가상메모리) + linu(Linux) + z(gzip 압축됨)
```
- **vm은 가상머신(Virtual Machine)이 아니라 "가상 메모리"** — 초창기 커널이 가상 메모리를 지원한 것을 강조.
- z는 gzip으로 압축되었다는 표시.

---

## STEP 3. 프로세스와 시스템 관리

### systemd
- **system + d(daemon)** = "시스템 전체를 관리하는 데몬".
- 커널이 부팅 후 **가장 먼저 직접 생성·실행**하는 사용자 영역의 첫 프로세스 (**PID 1**).
- 서비스 관리(`systemctl`), 로그(`journald`), 타이머, 마운트, 네트워크 등 시스템 전반을 관리.

### 시스템 vs 서비스 vs 유닛
- **서비스(Service)**: `sshd`, `nginx` 같은 개별 백그라운드 프로그램(데몬).
- **시스템(System)**: 서비스 + 마운트 + 소켓 + 타이머 + 부팅 순서 등 컴퓨터가 정상 작동하기 위한 모든 것.
- **유닛(Unit)**: systemd가 관리하는 모든 대상의 공통 단위. 서비스·마운트·타이머·소켓이 각각 유닛의 한 종류.
  - 서비스 = `.service` 유닛

### 데몬(daemon)이란?
- 그리스 신화의 "다이몬(보이지 않게 자기 역할을 수행하는 존재)"에서 유래.
- 사용자 눈에 안 보이는 **백그라운드에서 계속 돌며 요청을 처리하는 프로그램**.
- 이름 끝에 `d`가 붙는 관례: `sshd`, `httpd`, `crond`, `systemd`, `kthreadd`.

### systemd vs systemctl (중요 패턴!)
- **systemd**: 실제로 백그라운드에서 계속 돌며 유닛을 관리하는 **데몬 그 자체** (PID 1).
- **systemctl**: systemd에게 명령을 전달하는 **도구(리모컨)**. 자체 관리 기능 없음.
- 계층은 상하 관계가 아니라 "몸통과 리모컨" 관계.

```bash
systemctl start nginx     # nginx 시작
systemctl status sshd     # 상태 확인
systemctl enable/disable  # 부팅 시 자동 시작 설정
journalctl -u 서비스명     # 특정 서비스 로그
```

### 커널이 직접 만드는 프로세스는 딱 2개
```
커널
 ├── systemd  (PID 1) ← 사용자 공간 프로세스들의 시조
 └── kthreadd (PID 2) ← 커널 공간 스레드들의 시조
```
- **kthreadd** (kernel thread daemon): 커널 내부 작업용 커널 스레드들을 생성.
- 둘은 **부모-자식이 아니라 형제** 관계. 각자 커널이 독립적으로 생성 (PPID=0).
- **그 외 모든 프로세스/데몬은 이 둘로부터 `fork()`(자기 복제)로 파생됨.**
- 커널 스레드는 `ps`에서 `[kthreadd]`처럼 대괄호로 표시됨.

### 포그라운드 / 백그라운드 / 데몬
| 구분 | 터미널 연결 | 키보드 입력 수신 |
|---|---|---|
| 포그라운드 | 연결됨 | 받음 (입력 자리 차지) |
| 백그라운드 (`&`, `bg`) | 연결됨 | 안 받음 (입력 자리만 안 차지) |
| 데몬 | **연결 자체 없음** | 해당 없음 |

- 포그라운드/백그라운드는 **쉘의 작업 제어(job control)** 개념. systemd 소관이 아님.
- 모든 프로세스를 실제로 스케줄링/관리하는 근본은 **커널**.

### 프로세스 상태 (ps의 STAT 컬럼)
| 상태 | 이름 | 의미 |
|---|---|---|
| **R** | Running | 실행 중 또는 실행 대기 |
| **S** | Sleep | 이벤트 대기 중, **시그널로 깨울 수 있음**(interruptible) |
| **D** | Disk Sleep | 하드웨어 I/O 대기 중, **시그널로 못 깨움**(uninterruptible). `kill -9`도 불가. 파일시스템 손상 방지를 위해 커널이 보호 |
| **T** | Stopped | `Ctrl+Z`나 디버거로 **정지**됨(CPU 안 씀) |
| **Z** | Zombie | 작업은 끝났으나 부모가 종료 정보를 아직 안 가져간 상태 |

- **D 상태 주의**: 정상이면 밀리초 단위로 지나감. 오래 지속되면 하드웨어/네트워크 스토리지 문제 신호.

### 좀비 프로세스와 wait()
- 자식이 `exit()` → 종료 정보만 프로세스 테이블에 남음(좀비).
- 부모가 `wait()`/`waitpid()` 호출 → 커널이 그 정보를 부모에게 전달하고 **프로세스 테이블에서 완전히 제거**.
- `wait()`은 "기다리다"가 아니라 "**자식의 종료 정보를 수거한다**"는 능동적 행위.
- **고아 프로세스(orphan)**: 부모가 먼저 죽으면 PID 1(systemd)이 입양해서 대신 `wait()` 처리.
- 좀비 정리 방법: `SIGCHLD` 시그널 핸들러 안에서 `waitpid()` 호출이 정석.

### 프로세스 조회 명령어
```bash
ps aux                # 전체 프로세스 (BSD 스타일) — 모든 상태 다 보임
ps -ef                # 전체 프로세스 (System V 스타일)
ps aux | grep ' D '   # 특정 상태 필터링
top / htop            # 실시간 상태 확인 (ps는 스냅샷 한 장)
pstree                # 부모-자식 트리 구조
```
- `a`=모든 사용자, `u`=사용자 중심 상세정보, `x`=제어 터미널 없는 프로세스(데몬)까지 포함.
- **TTY** (TeleTYpewriter): 옛날 물리 입출력 기계 이름에서 유래. `ps`의 TTY 컬럼이 `?`면 터미널 연결 없음(데몬).

### 작업 제어 (Job Control)
```bash
sleep 100 &     # 백그라운드 실행 → [1] 12345 (작업번호 / PID)
jobs            # 작업 목록
fg %1           # 1번 작업을 포그라운드로
bg %1           # 1번 작업을 백그라운드에서 재개
Ctrl+Z          # 포그라운드 작업을 정지(T 상태)
```
- **작업번호(Job Number)**: 그 터미널 세션 안에서만 유효. `%` 붙여서 지정.
- **PID**: 시스템 전체에서 고유.
- ⚠️ `Ctrl+Z` 직후는 "백그라운드 실행"이 아니라 **정지(T)** 상태. `bg`를 쳐야 실제 실행(R/S)됨.

### 프로세스 종료 (kill)
```bash
kill 1234       # 기본값 SIGTERM(15) — 정중한 종료 요청
kill -15 1234   # SIGTERM (위와 동일)
kill -9 1234    # SIGKILL — 강제 즉시 종료
kill %1         # 작업번호로 지정
```

| | SIGTERM (15) | SIGKILL (9) |
|---|---|---|
| 성격 | 정중한 종료 요청 | 강제 즉시 종료 |
| 프로세스가 정리작업 가능? | 가능 | 불가능 |
| 무시/커스텀 가능? | 가능 | 불가능 |
| 기본값? | **kill의 기본** | `-9` 명시 필요 |

- ⚠️ **`kill`만 치면 기본은 SIGTERM(15)**, SIGKILL 아님.
- ⚠️ **D 상태 프로세스는 `kill -9`로도 못 죽임**.
- 정석: 먼저 SIGTERM → 안 죽으면 최후로 SIGKILL.

### 시그널과 상태 전환
- S 상태 프로세스에 `kill`을 보내면 → **시그널 자체가 깨우는 트리거** → S→R로 전환 → 종료 처리 실행.
- "먼저 깨우고 kill"이 아니라 "kill 신호 = 깨움". 그 R 상태는 매우 짧아 `ps`로 포착하기 어려움.
- D 상태는 이 "시그널 도착 → S→R 전환" 자체를 커널이 차단.

### 대표 시그널
| 번호 | 이름 | 의미 |
|---|---|---|
| 1 | SIGHUP | 터미널 연결 끊김 |
| 2 | SIGINT | `Ctrl+C` 인터럽트 |
| 9 | SIGKILL | 강제 즉시 종료(무시 불가) |
| 15 | SIGTERM | 정중한 종료 요청(기본값) |
| 19 | SIGSTOP | 강제 일시정지 |

---

## STEP 4. 파일시스템 구조와 마운트

### 리눅스 디렉토리 구조 (FHS)
| 디렉토리 | 역할 |
|---|---|
| `/boot` | 커널, GRUB 설정, initrd 등 부팅 파일 |
| `/bin` | 기본 명령어(`ls`, `cp`, `cat`) |
| `/sbin` | 시스템 관리 명령어(root용) |
| `/etc` | 시스템 설정 파일 |
| `/home` | 각 사용자 홈 디렉토리 |
| `/root` | root 전용 홈 디렉토리 |
| `/var` | 자주 변하는 데이터(로그 `/var/log` 등) |
| `/tmp` | 임시 파일 (Sticky Bit `1777`) |
| `/usr` | 설치된 프로그램·라이브러리 대부분 |
| `/lib` | 시스템 필수 라이브러리 |
| `/dev` | 장치(device) 파일 — `/dev/sda1` 등 |
| `/proc` | 커널이 실시간 생성하는 가상 파일시스템(프로세스 정보) |
| `/sys` | 커널·하드웨어 상태 가상 파일시스템 |
| `/mnt`, `/media` | 외부 저장장치 마운트 위치 |
| `/opt` | 서드파티 수동 설치 소프트웨어 |

- **`/dev`, `/proc`, `/sys`는 디스크 저장이 아니라 커널이 메모리에 실시간 생성**(재부팅 시 새로 생성).
- 그 외(`/etc`, `/home` 등)는 실제 디스크에 저장, 재부팅해도 유지.

### 장치 이름 규칙
```
/dev/sda1
     │││└─ 파티션 번호
     ││└── 몇 번째 디스크인지 (a, b, c...)
     │└─── Disk
     └──── SCSI (Small Computer System Interface)
```
- `/dev/sda` = 첫 번째 디스크, `/dev/sdb` = 두 번째 디스크.
- `/dev/sda1` = sda 디스크의 1번 파티션.
- NVMe SSD는 `/dev/nvme0n1` 같은 다른 규칙 사용.

### 마운트(Mount) — 핵심 개념
- **마운트 = 저장장치의 파일시스템을 특정 디렉토리 경로에 연결해 접근 가능하게 만드는 것.**
- **복사가 아님!** 파일이 옮겨오는 게 아니라 "접근 경로만 이어붙이는 것".
```bash
sudo mount /dev/sdb1 /mnt/usb   # USB를 /mnt/usb에 연결
sudo umount /mnt/usb            # 연결 해제(파일은 USB에 그대로)
```
- 루트(`/`)조차도 사실은 특정 디스크 파티션이 `/`에 마운트된 것.
- `/mnt/usb/photo.jpg`를 수정하면 → USB 안의 실제 파일이 수정됨(사본이 아님).

### 드라이버 vs 마운트 (순서)
```
1. USB 꽂음
2. 커널이 장치 인식 + 드라이버가 하드웨어와 통신 (드라이버 처리)
3. 커널이 /dev/sdb1로 노출
4. mount로 /dev/sdb1을 /mnt/usb에 연결 (마운트)
```
- **드라이버**: "이 하드웨어와 어떻게 통신하는지 아는 번역기 코드".
- **마운트**: 통신 가능한 상태에서 "파일시스템을 디렉토리에 끼워넣는 것".

### 디스크 → 파일시스템 → 마운트 → 디렉토리 (전체 그림)
| 용어 | 의미 |
|---|---|
| 디스크 파티션 | 물리적 저장 공간(raw 데이터) |
| 파일시스템 | 그 공간을 파일/폴더로 정리하는 **형식/규칙**(ext4, NTFS 등) |
| 마운트 | 정리된 파일시스템을 디렉토리 경로에 연결 |
| 디렉토리 | 터미널에서 `ls`, `cd`로 오가는 경로 |

- 커널이 하드웨어 raw 데이터를 "파일/폴더"로 **번역**하고, 쉘/터미널은 그 결과에 접근하는 창구.

### LVM (Logical Volume Manager)
파티션을 유연하게 관리하기 위한 중간 계층.
```
물리 파티션(PV) → 볼륨 그룹(VG) → 논리 볼륨(LV)
```
| 단계 | 역할 |
|---|---|
| PV (Physical Volume) | 실제 파티션을 LVM 원재료로 등록 |
| VG (Volume Group) | 여러 PV를 묶은 "용량 풀" |
| LV (Logical Volume) | 풀에서 잘라낸 가상 파티션 (실제 `/`에 마운트) |
- 장점: 디스크 추가 시 유연한 용량 확장, 스냅샷 등.

### 디스크/메모리 확인 명령어
```bash
df -h        # Disk Free — 마운트된 파일시스템 용량/사용량
du -sh 폴더   # Disk Usage — 특정 폴더/파일 용량
free -h      # 메모리(RAM) 사용 현황
lsblk        # list block device — 블록 장치(디스크/파티션) 트리 구조
fdisk -l     # 파티션 테이블 조회
```

- **df vs free**: df는 **디스크(저장장치)**, free는 **메모리(RAM)**. 완전히 다른 하드웨어.
  - 디스크 = 영구 저장(전원 꺼도 유지), 메모리 = 임시 작업 공간(전원 끄면 사라짐).
- **df vs du**: df는 파일시스템 전체, du는 특정 파일/폴더.
- **fdisk**: `-l`은 조회만. 생성/삭제는 옵션 없이 `fdisk /dev/sda`로 대화형 모드 진입 후 `n`/`d`/`w` 등 사용.

### /etc/fstab
- **Filesystem Table** — 부팅 시 자동 마운트할 파일시스템 목록 설정 파일.
- 잘못 수정하면 **부팅 불능**이 될 수 있어 수정 전 백업 필수:
```bash
sudo cp /etc/fstab /etc/fstab.bak
```

---

## STEP 5. 파일 권한(Permission)

### 권한 기본 구조
```bash
ls -l   # -l = long listing format (상세 정보)
# -rwxr-xr--  ...
```
- 맨 앞 1글자: `-`(파일), `d`(디렉토리)
- 나머지 9글자: 3글자씩 소유자/그룹/기타의 rwx

| 대상 | 기호 | 의미 |
|---|---|---|
| 소유자 | **u** | user (그 파일 소유자) |
| 그룹 | **g** | group (소유자의 그룹 구성원) |
| 기타 | **o** | **others** (그 외 나머지 전부) — ⚠️ owner 아님! |

- 권한 종류: **r**(read=4), **w**(write=2), **x**(execute=1)

### chmod (change mode)
```bash
chmod 755 file       # rwxr-xr-x (숫자 방식)
chmod u+x file       # 소유자에 실행권한 추가 (문자 방식)
chmod g-w file       # 그룹에서 쓰기권한 제거
```
- **mode** = 권한 정보를 나타내는 값(예: `755`, `rwxr-xr-x`). `chmod`는 그 값을 바꾸는 것.

**숫자 계산 (r=4, w=2, x=1)**
```
7 = 4+2+1 = rwx
5 = 4+1   = r-x
6 = 4+2   = rw-
```

### 파일 vs 디렉토리 삭제/실행 권한 포인트
- 실행 권한(`x`)은 **프로그램/스크립트**에만 필요. 설정 파일(`.yaml`, `.conf`)에는 불필요.
- 파일 **내용 수정**은 그 파일의 `w` 권한, 파일 **삭제/이름변경**은 그 파일이 든 **디렉토리**의 `w` 권한으로 결정.

### 특수 권한 (4자리)
```bash
chmod 4755 file   # SetUID
chmod 2770 dir    # SetGID
chmod 1777 dir    # Sticky Bit
chmod 3770 dir    # SetGID + Sticky
```

| 값 | 이름 | 파일에 걸면 | 디렉토리에 걸면 |
|---|---|---|---|
| **4000** | SetUID | 실행 시 **소유자 권한**으로 동작 | **효과 없음** |
| **2000** | SetGID | 실행 시 **소유 그룹 권한**으로 동작 | 새 파일이 **디렉토리 그룹** 상속 |
| **1000** | Sticky | (거의 안 씀) | **삭제/이름변경을 파일 소유자만** 가능 |

- **SetUID 원리**: 걸린 실행 파일은 누가 실행하든 소유자 권한으로 동작 (예: `passwd`가 root 소유+SetUID).
  - 보안 위험: `/bin/bash`에 SetUID를 걸면 `bash -p`로 누구나 root 획득(백도어).
  - `bash -p`의 `-p`(privileged): bash가 SetUID 시 스스로 권한을 낮추는 안전장치를 끔.
  - SetUID 파일 점검: `find / -perm -4000 -type f 2>/dev/null`
- **SetGID(디렉토리)**: 공유 폴더에서 새 파일 그룹을 자동 통일. 소유자(user)는 안 바뀌고 **그룹만** 상속.
- **Sticky Bit**: **삭제/이름변경만** 제한, **내용 수정은 제한 안 함**. `/tmp`(`1777`)가 대표 사례.

**공유 폴더 전형 패턴 (`3770`)**
```bash
sudo chmod 3770 /data/shared/
```
- `770`: dev 그룹 전체 rwx, 나머지 접근 불가
- SetGID: 새 파일이 dev 그룹 소유가 되도록 통일
- Sticky: 남의 파일은 삭제 못 하게

**자주 쓰는 권한 조합**
| 조합 | 의미 | 용도 |
|---|---|---|
| `600` | rw------- | SSH 개인키, 민감 설정 파일 |
| `644` | rw-r--r-- | 일반 파일 기본값 |
| `700` | rwx------ | 개인 스크립트/폴더 |
| `755` | rwxr-xr-x | 실행 스크립트/디렉토리 기본값 |
| `1777` | Sticky+rwxrwxrwx | `/tmp` |
| `3770` | SetGID+Sticky | 그룹 공유 디렉토리 |
| `4755` | SetUID+rwxr-xr-x | `passwd` 같은 실행 파일 |

### umask
- 새 파일/디렉토리 생성 시 **차단할(빼는) 권한**을 정하는 값 (부여가 아님!).
```
파일 기본값 666 - umask 022 = 644
디렉토리 기본값 777 - umask 022 = 755
```
```bash
umask        # 현재 값 확인 (예: 0022)
umask 027    # 더 엄격하게 설정
```

### chown (change owner)
```bash
chown root:dev /data/shared/   # 소유자=root, 그룹=dev
chown alice file               # 소유자만 변경
chown :dev file                # 그룹만 변경
```
- `소유자:그룹` 문법. "root에서 dev로 바꾸는 것"이 아니라 "소유자·그룹을 지정 설정"하는 것.

---

## STEP 6. 사용자와 그룹 관리

### 기본 그룹 vs 보조 그룹
- **기본 그룹(Primary)**: 계정 생성 시 자동 배정되는 딱 하나. 새 파일 생성 시 기본 소유 그룹.
- **보조 그룹(Secondary)**: 기본 외 추가로 속하는 그룹들(여러 개 가능).

### 사용자/그룹 명령어
```bash
sudo useradd -m alice      # 계정 생성 (-m: 홈 디렉토리 생성)
sudo userdel alice         # 계정 삭제
sudo usermod -aG dev alice # 보조 그룹 추가
sudo passwd alice          # 비밀번호 설정
id                         # uid/gid/그룹 확인 (실무 최다 사용)
groups                     # 속한 그룹 목록
cat /etc/group             # 전체 그룹 정의
```

- **`useradd -m`의 `-m`**: make home directory. RHEL 계열은 이 옵션 없으면 홈 디렉토리 안 생김.
- **`useradd` vs `adduser`**:
  - `useradd`: 저수준, 옵션 명시 필요, 모든 배포판 표준.
  - `adduser`: 고수준 스크립트, 대화형으로 알아서 처리(Debian/Ubuntu 계열). RHEL엔 없거나 심볼릭 링크.
  - 자동화/호환성엔 `useradd`, 개인 편의엔 `adduser`.

### usermod -aG (⚠️ 시험 단골 함정)
```bash
sudo usermod -aG dev alice   # ✅ 안전 (기존 그룹 유지 + 추가)
sudo usermod -G dev alice    # ⚠️ 위험 (기존 보조 그룹 다 사라지고 덮어씀)
```
- `-a` = append(추가), `-G` = 보조 그룹.
- **리다이렉션 비유**: `-aG`는 `>>`(추가), `-G`만은 `>`(덮어쓰기).
- `-G`의 글자는 **G**roup에서 왔지만, 대문자 `-G`는 관례적으로 "보조 그룹"을 다룸(소문자 `-g`는 기본 그룹).

### sudo, su, 권한
- **sudo** (superuser do): 이 명령 한 줄만 관리자 권한으로 실행. 끝나면 일반 권한 복귀.
- **su** (switch user): 아예 다른 사용자(보통 root)로 전환, exit 전까지 유지.
- 보안상 계속 root로 있는 `su`보다 필요할 때만 빌리는 `sudo` 권장.
- **sudo 그룹 ≠ root**: sudo를 쓸 "자격"을 가진 그룹. root는 최고 권한 계정 자체(uid=0).
- **GID 숫자(예: 27)는 고정 아님** — 배포판/설치마다 다름. 이름(`sudo`, `wheel`)으로 판단.
  - RHEL 계열은 관리자 그룹으로 `sudo` 대신 `wheel` 사용.

---

## STEP 7. 파일·텍스트 다루기 명령어

### 파일/디렉토리 조작
```bash
ls -l -a -h          # 목록(상세/숨김/읽기좋은 단위)
cd, pwd              # 이동, 현재 경로
cp -r src/ backup/   # 복사 (-r: 재귀)
mv                   # 이동/이름변경
rm -r -f             # 삭제 (-r: 재귀, -f: 강제)
mkdir -p /a/b/c      # 디렉토리 생성 (-p: 부모 자동 생성, 이미 있어도 에러 X)
find / -name "파일명"  # 파일 찾기
```

- **`-r`(recursive, 재귀)**: 하위 폴더 안의 하위 폴더까지 끝까지 다 처리.
  - `cp`/`rm`은 **대상이 디렉토리이기만 하면** `-r` 필요(중첩 여부 무관).
- **`-p`(mkdir)**: parents. 상위 디렉토리 없으면 자동 생성. (참고: `cp -p`의 `-p`는 preserve로 의미 다름 — 명령어마다 다르니 관용구로 암기)
- **`rm -rf` 주의**: 되돌릴 수 없는 완전 삭제. 휴지통 없음.

### 텍스트 확인
```bash
cat file             # 내용 출력 (concatenate)
cat > memo.txt       # 키보드 입력을 파일로 저장 (Ctrl+D로 종료)
less file            # 한 화면씩 (more의 개선판, "less is more" 말장난)
head / tail          # 앞/뒤 부분
tail -f              # 실시간 추적
grep '패턴' file      # 검색 (Globally search a Regular Expression and Print)
wc -l                # 줄 수 (Word Count, -l=lines)
```

- **cat**은 입력이 파일이면 파일 내용, 없으면 키보드(표준 입력)를 읽음. `cat > 파일`은 키보드→파일 저장 트릭.

### 파이프와 리다이렉션
```bash
ls | grep 'abc'          # 파이프: 앞 명령 출력 → 뒤 명령 입력
ls | grep 'abc' | wc -l  # 여러 개 연결 가능
ls > list.txt            # 표준출력을 파일로 (덮어쓰기)
ls >> list.txt           # 파일에 추가(append)
```
- **표준 출력(stdout)**: 명령어 결과가 기본적으로 나가는 곳(기본은 화면).
- 리눅스 3대 입출력: stdin(키보드), stdout(화면), stderr(화면).
- **파이프(`|`)**: 출력 → 다른 **명령어**의 입력. **리다이렉션(`>`)**: 출력 → **파일**.

### 원격 파일 전송: scp vs rsync
```bash
scp file.txt user@server:/path/     # secure copy — 통째로 복사
rsync -avz file.txt user@server:/path/  # remote sync — 차이(delta)만 전송
```
| | scp | rsync |
|---|---|---|
| 방식 | 통째로 복사 | 차이만 전송 |
| 재실행 | 매번 전체 재전송 | 바뀐 부분만 |
| 용도 | 한 번 빠르게 | 반복 백업/대용량 동기화 |
- rsync 옵션: `-a`(속성 보존), `-v`(상세 출력), `-z`(압축 전송).

---

## STEP 8. 네트워크 기초

### OSI 7계층
```
보낼 때: 7 → 1 (캡슐화: 계층마다 헤더 포장을 덧씌움)
받을 때: 1 → 7 (역캡슐화: 포장을 하나씩 벗겨냄)
```
- 편지를 겹겹이 포장(보낼 때) → 바깥부터 뜯음(받을 때)이라 순서가 반대.

| 계층 | 이름 | 핵심 | 데이터 단위 |
|---|---|---|---|
| 7 | 응용 | HTTP(S), SSH, FTP, DNS | 데이터 |
| 6 | 표현 | 포맷/암호화(SSL/TLS), 압축 | 데이터 |
| 5 | 세션 | 통신 세션 수립·복구 (소켓, API) | 데이터 |
| 4 | 전송 | 신뢰성/포트 (TCP/UDP, 포트번호) | 세그먼트(TCP)/데이터그램(UDP) |
| 3 | 네트워크 | 라우팅 (라우터, IP) | 패킷 |
| 2 | 데이터링크 | LAN 내 전달 (MAC, 스위치) | 프레임 |
| 1 | 물리 | 전기신호(0,1) (랜선, 광케이블) | 비트 |

### 데이터 단위 정리
- **패킷(Packet)**: 3계층에서 IP 헤더가 붙은 데이터 단위.
- **데이터그램(Datagram)**: 4계층에서 **UDP**일 때의 이름(TCP는 세그먼트).
- **IP**: 데이터 단위가 아니라 3계층의 **프로토콜(규약)** 이름.
  - **IP(프로토콜)** = 주소 표기·라우팅 규약 자체 / **IP 주소** = 그 규약에 따라 부여된 구체적 주소값. (같은 IP, 문맥 차이)

### TCP vs UDP (4계층)
| | TCP (등기우편) | UDP (엽서) |
|---|---|---|
| 연결 | 먼저 연결(핸드셰이크) | 그냥 바로 보냄 |
| 도착 확인 | 확인 응답(ACK) | 확인 안 함 |
| 유실 시 | 재전송 | 그냥 없어짐 |
| 속도 | 느림 | 빠름 |
| 용도 | 웹/파일/이메일(정확성) | 게임/화상통화/DNS(속도) |
- 포트가 "어디로"라면, TCP/UDP는 "어떻게 배달할지(성격)".

### 세션 계층 개념
- **세션**: 통신 시작~끝까지의 "하나의 대화 단위"(전화 통화 한 번).
- **소켓(Socket)**: 통신 창구(전화기). "IP 주소 + 포트 번호" 조합.
- **API**: 소켓/세션을 다루기 위해 OS가 제공하는 함수들(`socket()`, `connect()` 등).

### 핸드셰이크 (TCP 3-way vs TLS)
```
1. TCP 3-way 핸드셰이크 (연결 통로 열기): SYN → SYN-ACK → ACK
   ↓
2. TLS 핸드셰이크 (암호화 준비): 인증서 검증 + 암호화 키 교환
   ↓
3. 암호화된 HTTPS 데이터 전송
```
- **TCP 핸드셰이크**: 연결 자체를 맺음(모든 TCP 통신).
- **TLS 핸드셰이크**: 신원 확인+암호화 준비(HTTPS 등 암호화 시). TCP 연결 후 진행.

### NIC (Network Interface Card)
- 네트워크 통신을 담당하는 인터페이스. **물리적**(랜카드) 또는 **가상**(소프트웨어)일 수 있음.
- 물리 NIC 없이도 가상 NIC로 통신 가능. **어떤 형태든 NIC가 있어야 네트워크 통신 가능.**
| 종류 | 예시 |
|---|---|
| 물리 NIC | `eth0`, `ens33` |
| 가상 NIC | `lo`(loopback), `docker0`, `tun0`(VPN) |
- 이름 `ens33` = "ethernet network slot" (물리 위치 기반 예측 가능한 이름).
- **이더넷(Ethernet)**: NIC 이름이 아니라 유선 통신 **표준 방식**. `eth0`의 `eth`가 여기서 옴.

### IP 주소 vs MAC 주소
- **MAC 주소**: 기기(NIC)에 새겨진 고유값, 고정(주민등록번호).
- **IP 주소**: DHCP로 동적 할당 가능(호텔 방 번호).
- **IP 새로 받는 타이밍**: 처음 연결, 임대(lease) 만료·갱신 실패, 다른 네트워크 이동, 수동 갱신.
  - "매번 다름"이 아니라 "바뀔 가능성 있음"(같은 네트워크면 대개 같은 IP 유지).

### 게이트웨이 (Default Gateway)
- 자신의 네트워크 밖으로 패킷을 보내주는 **라우터의 IP 주소**.
- **패킷**: 개별 데이터 조각. **트래픽**: 패킷들의 전체 흐름/양(도로의 교통량).

### ip 명령어 (link / addr / route)
```bash
ip link show        # 인터페이스 자체 (UP/DOWN, MAC)
ip addr show        # 인터페이스의 IP 주소
ip route show       # 라우팅 테이블 (경로 규칙)
sudo ip link set ens33 up/down   # 특정 NIC 활성/비활성
```
| 객체 | 다루는 것 | 비유 |
|---|---|---|
| link | 인터페이스 자체(켜짐/MAC) | 문 자체 |
| addr | 인터페이스의 IP | 문에 붙은 주소 팻말 |
| route | 목적지별 경로 규칙 | 집 안 이정표 |

- `default via 192.168.1.1 dev eth0`:
  - `default`: 특별 규칙 없는 모든 목적지
  - `via 192.168.1.1`: 게이트웨이 경유
  - `dev eth0`: 나갈 때 사용할 인터페이스(장치)

### ip vs nmcli
- **`ip link show`**: 커널과 직접 통신, 저수준 정보(상태/MAC/MTU).
- **`nmcli device show`**: NetworkManager(데몬)를 통한 고수준 정보(IP/게이트웨이/DNS/프로필).
- systemd↔systemctl과 같은 "데몬↔CLI 도구" 구조.

### 방화벽: iptables vs UFW
- **iptables**: 커널 내장 방화벽/패킷 필터링 저수준 도구(강력하지만 복잡).
- **UFW** (Uncomplicated Firewall): iptables를 쉽게 제어하는 인터페이스("복잡하지 않은" 방화벽).
```bash
sudo ufw allow 22            # UFW (간단)
# iptables -A INPUT -p tcp --dport 22 -j ACCEPT  (복잡)
```
- **iptables vs 라우팅 테이블**: iptables=보안 검문(허용/차단), route table=길찾기(경로 결정). 완전히 다른 목적.

### netplan / cloud-init (Ubuntu 서버)
- **`/etc/netplan/*.yaml`**: 실제 네트워크 설정 내용(IP/게이트웨이/DNS). netplan이 읽어서 적용.
  - 민감 정보라 `chmod 600`(소유자만 rw). 실행 파일이 아니라 실행 권한 불필요.
- **`/etc/cloud/cloud.cfg.d/99-disable-network-config.cfg`**: cloud-init에게 "네트워크 설정 건드리지 마"라고 지시.
  - 없으면 재부팅 시 cloud-init이 수동 설정을 덮어쓸 수 있음.

---

## 핵심 "데몬 ↔ CLI 도구" 패턴 정리 (반복 등장!)

| 저수준 (실제 작업 수행) | 고수준 (쉬운 인터페이스) |
|---|---|
| `systemd` | `systemctl` |
| `NetworkManager` | `nmcli` |
| `iptables` | `ufw` |
| `dockerd` | `docker` |

- 실제 일을 하는 백그라운드 데몬 + 그걸 편하게 조작하는 CLI 도구로 나뉘는 구조.
- 리눅스 생태계 전반의 흔한 설계 방식. 비슷한 이름 쌍을 만나면 이 구조를 떠올릴 것.

---

## 시험 대비 — 헷갈리기 쉬운 함정 총정리

| 함정 | 정답 |
|---|---|
| `o`는 owner다? | ❌ **others**(그 외 나머지). owner는 `u`(user) |
| umask 값이 부여되는 권한이다? | ❌ **차단(빼는)** 권한 |
| SetUID를 디렉토리에 걸면 효과 있다? | ❌ 효과 없음 (디렉토리는 **SetGID**만 의미) |
| `usermod -G`만 써도 그룹 잘 추가됨? | ❌ 기존 보조 그룹 다 날아감. **`-aG`** 필수 |
| `kill 1234`는 강제 종료다? | ❌ 기본은 **SIGTERM(15)**, 정중한 요청 |
| `kill -9`는 모든 프로세스를 죽인다? | ❌ **D 상태**는 SIGKILL도 못 죽임 |
| `Ctrl+Z` 하면 백그라운드 실행된다? | ❌ **정지(T)** 상태. `bg` 쳐야 실행 |
| 마운트는 파일을 복사하는 것이다? | ❌ 접근 경로 연결(복사 아님) |
| 데몬은 터미널에 연결된 백그라운드다? | ❌ 데몬은 **터미널 연결 자체 없음** |
| cp로 디렉토리 복사 시 중첩일 때만 -r? | ❌ **디렉토리이기만 하면** 항상 -r |
| systemctl이 systemd를 관리하는 상위다? | ❌ 대등한 "리모컨↔몸통". 실제 관리는 systemd |
| vmlinuz의 vm은 가상머신이다? | ❌ **가상 메모리**(virtual memory) |
| IP(프로토콜)와 IP 주소는 다른 IP다? | ❌ 같은 IP. 규약 자체 vs 그 규약이 부여한 주소값 |

---

## 부록 — 자주 나오는 약자 모음

| 약자 | 풀이 |
|---|---|
| SSH | Secure Shell |
| grep | Globally search a Regular Expression and Print |
| wc | Word Count |
| cp | Copy |
| rm / mkdir | Remove / Make Directory |
| chmod / chown | Change Mode / Change Owner |
| sudo / su | superuser do / switch user |
| ps | Process Status |
| df / du | Disk Free / Disk Usage |
| lsblk | list block device |
| fstab | Filesystem Table |
| TTY | TeleTYpewriter |
| SCSI | Small Computer System Interface |
| BIOS | Basic Input Output System |
| UEFI | Unified Extensible Firmware Interface |
| GRUB | GRand Unified Bootloader |
| LVM (PV/VG/LV) | Logical Volume Manager (Physical Volume/Volume Group/Logical Volume) |
| POSIX | Portable Operating System Interface |
| RHEL | Red Hat Enterprise Linux |
| DNF | Dandified YUM |
| YUM | Yellowdog Updater Modified |
| PPA | Personal Package Archive |
| NIC | Network Interface Card |
| MAC | Media Access Control |
| IP | Internet Protocol |
| TCP / UDP | Transmission Control Protocol / User Datagram Protocol |
| DHCP | Dynamic Host Configuration Protocol |
| UFW | Uncomplicated Firewall |
| rsync | remote sync |
| scp | secure copy |
| SSL/TLS | Secure Sockets Layer / Transport Layer Security |

