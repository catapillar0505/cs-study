# JPA N+1 / 캐시 / 이벤트 스터디 질문지 + 답지

> 기초 → 중급 → 심화 순으로 구성. 각 질문을 먼저 답해보고 답을 확인하세요.

---

## 기초

---

**Q1. N+1 문제란 무엇인가? 이름이 "N+1"인 이유는?**

<details>
<summary>답 보기</summary>

JPA에서 연관관계가 있는 엔티티를 조회할 때, **1번의 쿼리로 N개의 결과를 가져왔는데 추가로 N번의 쿼리가 더 발생**하는 성능 문제다.

**이름의 유래**: 처음 목록을 가져오는 쿼리 "1번" + 각 결과마다 연관 엔티티를 가져오는 쿼리 "N번" = 총 **N+1번**의 쿼리가 실행되기 때문이다.

```
Team 5개 조회 → 1번
각 Team의 Member 조회 → 5번
총 = 1 + 5 = 6번

Team 1000개라면 → 1 + 1000 = 1001번 🚨
```

</details>

---

**Q2. N+1 문제의 근본 원인은 무엇인가?**

<details>
<summary>답 보기</summary>

**LAZY 로딩 + 반복문에서 연관 엔티티 접근**의 조합이 원인이다.

`FetchType.LAZY`는 연관 엔티티를 **실제로 사용하는 시점**에 쿼리를 날린다.  
`findAll()`로 목록을 가져올 때는 연관 엔티티를 조회하지 않고, 루프에서 연관 필드에 접근하는 순간 각각 쿼리가 실행된다.

```java
List<Team> teams = teamRepository.findAll();  // 쿼리 1번
for (Team team : teams) {
    team.getMembers(); // 팀마다 쿼리 1번씩 — 여기서 N번 발생
}
```

</details>

---

**Q3. `FetchType.EAGER`로 바꾸면 N+1 문제가 해결되는가?**

<details>
<summary>답 보기</summary>

**아니다.** EAGER로 바꿔도 N+1 문제는 여전히 발생한다.

JPQL로 `findAll()`을 실행하면, JPA는 JPQL 결과(Team 목록)를 먼저 가져온 후 EAGER 설정에 따라 각 Team마다 Member를 **개별 쿼리**로 추가 조회한다.

EAGER는 "즉시 가져온다"는 것이지, "한 번의 쿼리로 가져온다"는 보장이 아니다.  
오히려 EAGER는 N+1 문제를 개발자가 인지하기 더 어렵게 만든다.

</details>

---

**Q4. N+1 해결 방법 3가지를 말하라.**

<details>
<summary>답 보기</summary>

1. **Fetch Join**: JPQL에서 `JOIN FETCH`를 사용해 한 번의 쿼리로 연관 엔티티를 함께 조회
2. **`@EntityGraph`**: Spring Data JPA 어노테이션으로 `@Query` 없이 Fetch Join 효과
3. **Batch Size**: IN 절을 사용해 여러 연관 엔티티를 한꺼번에 조회 (페이징 시 권장)

| 방법 | 쿼리 횟수 | 페이징 가능 |
|---|---|---|
| Fetch Join | 1번 | ❌ (컬렉션의 경우) |
| @EntityGraph | 1번 | ❌ (컬렉션의 경우) |
| Batch Size | 2번 이상 | ✅ |

</details>

---

**Q5. `@EntityListeners`와 더티 체킹(Dirty Checking)은 어떤 차이가 있는가?**

<details>
<summary>답 보기</summary>

감지하는 대상 자체가 다르다.

| 구분 | 더티 체킹 | `@EntityListeners` |
|---|---|---|
| 감지 대상 | 엔티티 **필드값 변화** | 엔티티 **생명주기 이벤트** |
| 결과 | DB UPDATE 자동 실행 | 등록한 콜백 메서드 실행 |
| 자동 입력 | `createdAt`, `createdBy` 등 **모름** | `@CreatedDate`, `@CreatedBy` 등 **자동 채움** |

더티 체킹은 **"뭐가 바뀌었는지"** 만 알고, **"언제 만들었는지", "누가 만들었는지"** 는 모른다.  
`@EntityListeners`는 그런 메타 정보를 **자동화**해주는 것이다.

</details>

---

**Q6. JPA 생명주기 이벤트 어노테이션 6가지와 발생 시점을 말하라.**

<details>
<summary>답 보기</summary>

| 어노테이션 | 발생 시점 |
|---|---|
| `@PrePersist` | `persist()` **직전** |
| `@PostPersist` | `persist()` **직후** |
| `@PreUpdate` | UPDATE **직전** |
| `@PostUpdate` | UPDATE **직후** |
| `@PreRemove` | `remove()` **직전** |
| `@PostRemove` | `remove()` **직후** |

`AuditingEntityListener`가 이 이벤트를 활용해 `@CreatedDate`, `@LastModifiedDate` 등을 자동으로 채워준다.

</details>

---

**Q7. `flush`와 `commit`의 차이는 무엇인가?**

<details>
<summary>답 보기</summary>

| 구분 | flush | commit |
|---|---|---|
| 하는 일 | 쓰기 지연 저장소의 SQL을 DB로 전송해 **실행** | 트랜잭션의 변경을 **영구 확정** |
| 외부 공개 여부 | 트랜잭션 내부에만 반영 (다른 트랜잭션에서 못 봄) | 다른 트랜잭션에서도 볼 수 있게 공개 |
| rollback 가능 여부 | flush 후에도 rollback 가능 | commit 후에는 rollback 불가 |

```
flush  → "DB야, 이 UPDATE문 실행해"   → DB가 실행함 (트랜잭션 내 임시 반영)
commit → "이제 확정해, 다른 사람도 봐도 돼" → 영구 반영
```

**순서**: commit 직전에 flush가 자동으로 선행된다.  
flush만 하고 commit 없이 rollback하면 실행된 쿼리도 사라진다.

</details>

---

## 중급

---

**Q8. Fetch Join의 기본 동작은 INNER JOIN인가 LEFT OUTER JOIN인가? 차이가 왜 중요한가?**

<details>
<summary>답 보기</summary>

기본 `JOIN FETCH`는 **INNER JOIN**이다.  
LEFT OUTER JOIN을 원하면 **`LEFT JOIN FETCH`** 로 명시해야 한다.

```java
// INNER JOIN FETCH (기본) — member가 없는 Team은 결과에서 제외됨
@Query("SELECT t FROM Team t JOIN FETCH t.members")

// LEFT JOIN FETCH — member가 없는 Team도 결과에 포함됨
@Query("SELECT t FROM Team t LEFT JOIN FETCH t.members")
```

**왜 중요한가?**  
`JOIN FETCH`를 쓰면 연관 엔티티가 없는 부모 엔티티가 결과에서 빠질 수 있다.  
예를 들어 "댓글이 하나도 없는 게시글"이 INNER JOIN FETCH로 조회 시 결과에서 누락된다.  
이런 경우 의도치 않은 데이터 손실이 발생하므로, 목적에 맞게 INNER/LEFT를 구분해야 한다.

</details>

---

**Q9. Fetch Join이 컬렉션 조회 시 페이징에 사용하기 어려운 이유는?**

<details>
<summary>답 보기</summary>

컬렉션 Fetch Join을 하면 JOIN 결과로 **데이터가 뻥튀기(중복 행)** 된다.

```
Team A — Member 3명이면:
INNER JOIN 결과 →  TeamA | Member1
                   TeamA | Member2
                   TeamA | Member3  ← 팀 A가 3행으로 뻥튀기됨
```

DB에서 `LIMIT`/`OFFSET`(페이징)을 적용하면 중복된 행 기준으로 잘라내기 때문에, **원하는 팀 개수만큼 정확하게 페이징할 수 없다.**

Hibernate는 이 경우 DB에서 페이징하지 않고 **전체 데이터를 메모리로 가져온 후 애플리케이션에서 페이징**하는 방식으로 처리한다(`HHH90003004` 경고 발생). 대용량 데이터에서는 심각한 메모리 문제로 이어진다.

**페이징이 필요하면 Batch Size를 사용해야 한다.**

</details>

---

**Q10. Batch Size 설정 후 쿼리가 어떻게 달라지는지 예시로 설명하라.**

<details>
<summary>답 보기</summary>

**설정 전 (N+1)**: Team 5개 조회 시 총 6번 쿼리
```sql
SELECT * FROM team;
SELECT * FROM member WHERE team_id = 1;
SELECT * FROM member WHERE team_id = 2;
-- ... 5번 반복
```

**설정 후 (Batch Size = 100)**: Team 5개 조회 시 총 2번 쿼리
```sql
SELECT * FROM team;
SELECT * FROM member WHERE team_id IN (1, 2, 3, 4, 5);
```

Team이 150개라면: 1번 + 2번(100개 IN + 50개 IN) = 3번.

```yaml
# application.yml — 전역 설정
spring.jpa.properties.hibernate.default_batch_fetch_size: 100
```

```java
// 특정 필드만 적용
@BatchSize(size = 100)
@OneToMany(mappedBy = "team")
private List<Member> members = new ArrayList<>();
```

페이징 + N+1 해결이 필요할 때 권장된다.

</details>

---

**Q11. `@GeneratedValue`를 `@Id` 없이 단독으로 사용하면 어떻게 되는가? 그 이유는?**

<details>
<summary>답 보기</summary>

**컴파일 오류가 발생한다.**

`@GeneratedValue`는 **PK 생성 전략**을 지정하는 어노테이션이기 때문에, PK임을 선언하는 `@Id`가 반드시 함께 있어야 한다.  
`@Id` 없이 PK 생성 전략만 선언하는 것은 의미가 없다.

반대로 `@Id`는 단독으로 사용할 수 있다. 이 경우 PK를 개발자가 직접 할당(수동 지정)하는 방식이다.

```java
@Id                                          // ✅ 단독 사용 가능 (직접 할당)
private String productCode;

@Id
@GeneratedValue(strategy = GenerationType.IDENTITY) // ✅ 함께 사용
private Long id;

@GeneratedValue(strategy = GenerationType.IDENTITY) // ❌ 컴파일 오류
private Long id;
```

</details>

---

**Q12. `GenerationType.IDENTITY` 전략에서 `persist()` 시 즉시 실행되는 쿼리와 지연되는 쿼리의 차이는?**

<details>
<summary>답 보기</summary>

**즉시 실행**: INSERT 쿼리만 즉시 나간다.  
**지연(쌓임)**: DELETE, UPDATE 등 나머지 쿼리는 모두 쓰기 지연 저장소에 쌓였다가 커밋 시점에 실행된다.

**이유**: IDENTITY 전략은 DB가 PK를 생성하므로, INSERT를 즉시 실행해야 DB로부터 PK 값을 받아와 영속성 컨텍스트에 등록할 수 있다. PK를 모르면 1차 캐시에 엔티티를 식별해서 저장할 수가 없기 때문이다.

반면 UPDATE와 DELETE는 이미 PK를 알고 있는 상태에서 발생하므로 기존 쓰기 지연 메커니즘이 그대로 적용된다.

</details>

---

**Q13. JPA 2차 캐시와 Spring `@Cacheable`의 차이를 설명하라.**

<details>
<summary>답 보기</summary>

| 구분 | JPA 2차 캐시 (`@Cache`) | Spring `@Cacheable` |
|---|---|---|
| 소속 | JPA/Hibernate 영역 | Spring AOP 영역 |
| 동작 방식 | EntityManagerFactory 레벨에서 **엔티티 객체**를 캐싱 | 메서드 호출 **결과(반환값)** 를 캐싱 |
| 캐시 저장소 | Ehcache, Infinispan 등 (JPA provider 종속) | Redis, Caffeine, ConcurrentMap 등 직접 선택 |
| 적용 대상 | 엔티티 객체 | 어떤 메서드든 가능 |
| 동작 원리 | `em.find()` 호출 시 내부적으로 자동 개입 | AOP 프록시가 메서드 호출을 가로채서 캐시 확인 |

```java
@Cacheable(value = "members", key = "#id")
public Member getMember(Long id) {
    return memberRepository.findById(id).orElseThrow();
}
```

이 코드는 JPA 1차/2차 캐시와 **무관**하게 Spring AOP가 메서드 결과를 Redis 같은 저장소에 캐싱하는 것이다.

**한 줄 요약**: 2차 캐시는 JPA 영속성 레이어의 엔티티 캐싱, `@Cacheable`은 Spring AOP가 메서드 결과를 캐싱 — **서로 다른 차원의 캐싱**이다.

</details>

---

## 심화

---

**Q14. 아래 코드의 N+1 문제를 세 가지 방법으로 각각 수정하라.**

```java
// Repository
List<Team> findAll();

// Service
List<Team> teams = teamRepository.findAll();
for (Team team : teams) {
    System.out.println(team.getName() + ": " + team.getMembers().size());
}
```

<details>
<summary>답 보기</summary>

**방법 1 — Fetch Join:**
```java
// Repository
@Query("SELECT t FROM Team t JOIN FETCH t.members")
List<Team> findAllWithMembers();
```
쿼리 1번. 단, 컬렉션 JOIN FETCH이므로 페이징 불가.  
멤버가 없는 팀도 포함하려면 `LEFT JOIN FETCH` 사용.

---

**방법 2 — @EntityGraph:**
```java
// Repository
@EntityGraph(attributePaths = {"members"})
List<Team> findAll();
```
`@Query` 없이 Fetch Join과 동일한 효과. LEFT OUTER JOIN으로 동작.

---

**방법 3 — Batch Size:**
```java
// application.yml
spring.jpa.properties.hibernate.default_batch_fetch_size: 100

// 또는 엔티티에 직접
@BatchSize(size = 100)
@OneToMany(mappedBy = "team")
private List<Member> members = new ArrayList<>();
```
쿼리 2번(Team 조회 1번 + IN절 Member 조회 1번).  
페이징이 필요한 경우 이 방법을 사용한다.

</details>

---

**Q15. 분산 서버 환경(서버 여러 대)에서 JPA 2차 캐시를 사용하면 어떤 문제가 발생하는가? 실무에서 어떤 대안을 사용하는가?**

<details>
<summary>답 보기</summary>

**문제 — 캐시 불일치(Cache Inconsistency):**  
2차 캐시는 각 서버의 **JVM 메모리**에 따로 존재한다.  
서버 A에서 데이터를 변경해도 서버 B의 2차 캐시는 갱신되지 않는다.  
서버 B에서 조회 시 오래된 데이터(stale data)가 반환된다.

```
서버A: 상품가격 1000원 → 2000원으로 변경 (서버A 2차캐시 갱신)
서버B: 상품가격 조회 → 2차캐시에서 1000원 반환 🚨 (불일치)
```

**실무 대안:**

| 상황 | 선택 |
|---|---|
| 분산 서버, 서버 간 일관성 중요 | **Redis** + `@Cacheable` |
| 단일 서버 또는 약간의 불일치 허용 | **Caffeine** (로컬 캐시, 빠르고 가벼움) |
| 거의 변하지 않는 참조 데이터 | 애플리케이션 시작 시 메모리 로딩 + Caffeine |

**결론**: 실무에서는 JPA 2차 캐시보다 **Redis 기반 캐싱이 압도적으로 많고**, 단일 서버에서는 Caffeine이 더 가볍고 명확하다.

</details>

---

**Q16. flush 후 rollback을 하면 DB 상태는 어떻게 되는가? flush와 commit이 각각 독립적으로 존재하는 이유는?**

<details>
<summary>답 보기</summary>

**flush 후 rollback 시 DB 상태:**  
flush는 트랜잭션 **내부에서** SQL을 실행한 것이다. 아직 커밋되지 않은 상태이므로, rollback하면 DB에 실행된 변경이 **모두 취소**되어 원래 상태로 돌아간다.

```
em.persist(product);   // 쓰기 지연 저장소에 쌓임
em.flush();            // INSERT SQL 실행 → DB에 반영됨 (트랜잭션 내)
tx.rollback();         // INSERT 취소 → DB 원상 복구
```

**둘이 독립적으로 존재하는 이유:**  
flush만 먼저 해야 하는 상황이 있기 때문이다.

```java
em.persist(product);
em.flush(); // 👈 이 시점에 flush해야 바로 아래 JPQL에서 방금 저장한 product가 보임
em.createQuery("SELECT p FROM Product p WHERE p.price > 1000")
  .getResultList(); // flush 없이 실행하면 방금 저장한 데이터가 DB에 없어서 못 찾을 수 있음
tx.commit();
```

JPQL 실행 전에 flush를 해야 DB와 영속성 컨텍스트의 상태가 일치하여 올바른 결과를 얻을 수 있다. (실제로 JPA는 JPQL 실행 전 자동으로 flush한다.)

</details>

---

**Q17. 아래 코드에서 `@CreatedDate`와 `@LastModifiedDate`가 자동으로 채워지려면 무엇이 추가로 필요한가?**

```java
@EntityListeners(AuditingEntityListener.class)
@Entity
public class Post {
    @CreatedDate
    private LocalDateTime createdAt;

    @LastModifiedDate
    private LocalDateTime updatedAt;
}
```

<details>
<summary>답 보기</summary>

**Spring Data JPA Auditing 활성화**가 필요하다.  
`@SpringBootApplication` 또는 `@Configuration` 클래스에 `@EnableJpaAuditing`을 추가해야 한다.

```java
@EnableJpaAuditing
@SpringBootApplication
public class Application {
    public static void main(String[] args) {
        SpringApplication.run(Application.class, args);
    }
}
```

이 어노테이션이 없으면 `AuditingEntityListener`가 등록되어 있어도 `@CreatedDate`, `@LastModifiedDate` 값이 자동으로 채워지지 않는다.

**`@CreatedBy`, `@LastModifiedBy`까지 사용하려면** `AuditorAware<T>` 구현체도 빈으로 등록해야 한다.

```java
@Bean
public AuditorAware<String> auditorProvider() {
    return () -> Optional.of(SecurityContextHolder.getContext()
        .getAuthentication().getName()); // 현재 로그인한 사용자
}
```

</details>

---

**Q18. 아래 두 캐싱 방식 중 어느 쪽이 더 적합한지 판단하고 이유를 설명하라.**

> 상황: 서버 3대로 운영 중인 서비스. "공통 코드 테이블(시/도 목록, 카테고리 목록)"을 캐싱하려 한다. 데이터는 관리자가 한 달에 한 번 정도 변경한다.

<details>
<summary>답 보기</summary>

**Redis + `@Cacheable`이 더 적합하다.**

**이유:**
1. **분산 서버 환경** → 서버별로 JVM 메모리에 따로 캐싱(JPA 2차 캐시, Caffeine)하면 한 서버에서 데이터를 변경해도 나머지 서버의 캐시는 갱신되지 않는 **불일치 문제**가 생긴다.
2. Redis는 모든 서버가 **공유하는 중앙 캐시 저장소**라서 한 서버에서 캐시를 무효화하면 나머지 서버도 즉시 최신 데이터를 조회한다.
3. 한 달에 한 번 변경이지만, 변경 시 즉시 반영이 필요하다면 Redis에서 `evict`로 캐시를 무효화하면 된다.

```java
@Cacheable(value = "commonCodes", key = "#groupCode")
public List<CommonCode> getCommonCodes(String groupCode) {
    return commonCodeRepository.findByGroupCode(groupCode);
}

@CacheEvict(value = "commonCodes", key = "#groupCode")
public void updateCommonCode(String groupCode, ...) { ... }
```

**만약 단일 서버라면**: Caffeine(로컬 캐시)이 네트워크 비용 없이 더 빠른 선택이다.

</details>

---

## 보너스 — 면접 단골 질문

---

**Q19. "N+1 문제를 해결했다"고 말할 때, 상황에 따라 Fetch Join과 Batch Size를 선택하는 기준을 설명하라.**

<details>
<summary>답 보기</summary>

**핵심 기준: 페이징 필요 여부**

| 상황 | 선택 | 이유 |
|---|---|---|
| 페이징 불필요 | **Fetch Join** | 1번의 쿼리로 해결. 가장 직관적. |
| 페이징 필요 | **Batch Size** | Fetch Join + 컬렉션 페이징은 메모리에서 전체 로딩 → OOM 위험 |
| 조인 복잡도가 높을 때 | **Batch Size** | 다중 컬렉션 Fetch Join은 `MultipleBagFetchException` 발생 |

**면접 심화 포인트:**  
Fetch Join은 컬렉션 조회 시 중복 행이 발생한다. Hibernate 5 이하에서는 `DISTINCT`를 JPQL에 추가해야 했지만, Hibernate 6(Spring Boot 3+)부터는 자동으로 중복이 제거된다.

**실무 공식:**
- 상세 페이지(단건 조회) → **Fetch Join**
- 목록 페이지(페이징) → **Batch Size** + `@PageableDefault`

</details>

---

**Q20. 아래 코드 각 줄에서 SQL이 실행되는 시점을 분석하라. (`GenerationType.IDENTITY` 전략 사용)**

```java
EntityTransaction tx = em.getTransaction();
tx.begin();                                           // (1)

Member m1 = new Member("Alice");
em.persist(m1);                                       // (2)

Member m2 = new Member("Bob");
em.persist(m2);                                       // (3)

m1.setName("Alice Updated");                          // (4)

em.remove(m2);                                        // (5)

tx.commit();                                          // (6)
```

<details>
<summary>답 보기</summary>

| 줄 | SQL 실행 여부 | 이유 |
|---|---|---|
| (1) `tx.begin()` | 없음 | 트랜잭션 시작만 함 |
| (2) `em.persist(m1)` | **INSERT 즉시 실행** | IDENTITY 전략 — DB가 PK를 생성해야 하므로 즉시 INSERT |
| (3) `em.persist(m2)` | **INSERT 즉시 실행** | 동일 이유 |
| (4) `m1.setName("Alice Updated")` | 없음 | 영속 상태의 필드 변경은 쓰기 지연 저장소에 UPDATE 예약만 됨 |
| (5) `em.remove(m2)` | 없음 | DELETE가 쓰기 지연 저장소에 예약됨 |
| (6) `tx.commit()` | **UPDATE + DELETE 실행** | flush 발생 → 쌓인 UPDATE(m1), DELETE(m2) SQL 실행 후 커밋 |

**최종 실행된 SQL 순서:**
```sql
-- (2): INSERT INTO member (name) VALUES ('Alice');
-- (3): INSERT INTO member (name) VALUES ('Bob');
-- (6) flush:
--   UPDATE member SET name='Alice Updated' WHERE id=1;
--   DELETE FROM member WHERE id=2;
-- commit
```

**핵심**: IDENTITY 전략에서 INSERT만 즉시 나가고, UPDATE/DELETE는 커밋 시점에 나간다.

</details>

