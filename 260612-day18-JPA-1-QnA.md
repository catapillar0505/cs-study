# JPA 스터디 질문지 + 답지

> 기초 → 중급 → 심화 순으로 구성. 각 질문을 먼저 답해보고 답을 확인하세요.

---

## 기초

---

**Q1. JPA란 무엇이고, 대표적인 구현체는?**

<details>
<summary>답 보기</summary>

JPA(Java Persistence API)는 ORM 기술을 사용하는 **인터페이스(표준 명세)** 다.  
대표적인 구현체는 **Hibernate** 이며, 우리가 JPA를 쓰면 내부적으로 Hibernate가 동작한다.

</details>

---

**Q2. "엔티티(Entity)"란 무엇인가?**

<details>
<summary>답 보기</summary>

DB 테이블과 **1:1 매핑되는 자바 객체**다. `@Entity` 어노테이션을 붙여 선언한다.  
이 어노테이션이 없으면 JPA는 해당 클래스를 일반 자바 객체로 취급하여 `persist()`가 동작하지 않는다.

</details>

---

**Q3. "영속성 컨텍스트(Persistence Context)"란 무엇인가?**

<details>
<summary>답 보기</summary>

**엔티티를 관리하고 저장하는 논리적 공간**이다.  
엔티티 매니저(EntityManager)가 영속성 컨텍스트를 관리한다.

</details>

---

**Q4. 엔티티의 4가지 생명주기를 순서대로 나열하고, 각각의 상태를 설명하라.**

<details>
<summary>답 보기</summary>

| 상태 | 설명 |
|---|---|
| **비영속 (Transient)** | `new`로 생성만 된 상태. JPA가 모르는 일반 자바 객체 |
| **영속 (Persistent)** | `em.persist()` 호출 후 영속성 컨텍스트(1차 캐시)가 관리하는 상태 |
| **준영속 (Detached)** | 영속 상태였다가 `detach()`, `clear()`, `close()` 등으로 분리된 상태 |
| **삭제 (Removed)** | `em.remove()` 호출 후 삭제 예정인 상태 |

</details>

---

**Q5. `GenerationType.IDENTITY`와 MySQL의 `AUTO_INCREMENT`는 어떤 관계인가?**

<details>
<summary>답 보기</summary>

`GenerationType.IDENTITY`는 **JPA 레벨의 전략 이름**이고,  
`AUTO_INCREMENT`는 **MySQL이 제공하는 실제 기능**이다.

`IDENTITY` 전략을 선언하면 JPA가 Dialect를 통해 해당 DB의 자동 증가 기능(`AUTO_INCREMENT`)으로 매핑한다.  
즉 MySQL 기준으로 "JPA의 IDENTITY = MySQL의 AUTO_INCREMENT를 사용하겠다는 선언"이다.

</details>

---

**Q6. `Dialect`는 무슨 역할을 하는가?**

<details>
<summary>답 보기</summary>

개발자가 작성한 DB 종속적이지 않은 코드(JPQL)를 **해당 DB에 맞는 실제 SQL로 번역**하는 역할을 한다.

```
JPQL → Dialect 확인(MySQLDialect, OracleDialect 등) → 실제 SQL 생성
```

</details>

---

## 중급

---

**Q7. `em.persist()`를 호출하면 즉시 INSERT SQL이 DB로 나가는가? 예외는?**

<details>
<summary>답 보기</summary>

**아니다.** `persist()`는 엔티티를 영속성 컨텍스트의 **쓰기 지연 SQL 저장소**에 쌓아두기만 한다.  
실제 INSERT는 `tx.commit()` 또는 `em.flush()` 시점에 한꺼번에 DB로 전송된다.

**예외 — `GenerationType.IDENTITY` 전략:**  
DB가 PK를 생성해야 하므로, `persist()` 호출 즉시 INSERT가 실행된다.  
(영속성 컨텍스트가 엔티티를 식별하려면 PK 값이 필요하기 때문)

</details>

---

**Q8. "flush"란 무엇이고, flush가 자동으로 발생하는 시점은 언제인가?**

<details>
<summary>답 보기</summary>

**flush**: 쓰기 지연 SQL 저장소에 쌓여있던 SQL들을 한꺼번에 DB로 전송하는 행위.  
"버퍼에 쌓인 것을 한 번에 비워서 내보낸다"는 의미다.

**flush 자동 발생 시점:**
1. `tx.commit()` 호출 시
2. JPQL 쿼리 실행 직전
3. `em.flush()` 직접 호출 시

> 주의: flush 후에도 **영속성 컨텍스트(1차 캐시)는 비워지지 않는다.** SQL만 전송되는 것.

</details>

---

**Q9. `detach()`, `clear()`, `close()`의 차이를 설명하라.**

<details>
<summary>답 보기</summary>

| 메서드 | 대상 | 효과 |
|---|---|---|
| `detach(entity)` | 특정 엔티티 1개 | 해당 엔티티만 준영속 상태로 분리 |
| `clear()` | 영속성 컨텍스트 전체 | 모든 엔티티를 준영속으로 만들고 컨텍스트를 비움. EntityManager는 살아있음 |
| `close()` | EntityManager 자체 | 영속성 컨텍스트 + EntityManager 완전 종료. 이후 사용 시 예외 발생 |

**실무에서 자주 쓰는 것은 `clear()`** — 대량 데이터 처리 시 1차 캐시가 무한정 커지는 것을 방지할 때 사용.

```java
for (int i = 0; i < 10000; i++) {
    em.persist(new Product(...));
    if (i % 100 == 0) {
        em.flush(); // SQL 전송
        em.clear(); // 메모리 확보
    }
}
```

</details>

---

**Q10. JPA에서 `@Setter`를 엔티티에 지양하는 이유 3가지 이상을 설명하라.**

<details>
<summary>답 보기</summary>

1. **객체 일관성(캡슐화) 훼손**: `setPrice(-1000)` 처럼 비즈니스 규칙 검증 없이 아무 값이나 넣을 수 있다.

2. **의도 불명확**: `setStock(product.getStock() - quantity)` 만 보면 재고를 줄이는 건지, 초기화하는 건지 알 수 없다. `decreaseStock(quantity)` 같은 명확한 메서드가 낫다.

3. **변경 추적 어려움**: 코드베이스 전체에서 어디서 값이 바뀌는지 찾기 힘들어져 디버깅이 어렵다.

4. **Dirty Checking과 얽힘**: JPA는 필드 변화를 감지해 자동으로 UPDATE를 날리는데, setter가 열려있으면 검증 없이 잘못된 값이 DB에 반영될 수 있다.

**대안**: `changePrice()`, `decreaseStock()` 처럼 **검증 로직을 포함한 비즈니스 메서드**를 사용한다.

</details>

---

**Q11. JPA 엔티티에 왜 기본 생성자(no-args constructor)가 필요한가? 그리고 왜 `public`이 아닌 `protected`로 설정하는가?**

<details>
<summary>답 보기</summary>

**기본 생성자가 필요한 이유:**  
JPA(Hibernate)는 DB에서 데이터를 조회한 후 **리플렉션(Reflection)** 으로 빈 객체를 먼저 만들고, 이후 필드에 값을 채워 넣는 방식으로 동작한다. 이때 기본 생성자가 없으면 "빈 객체"를 만들 수 없어 오류가 발생한다.

**`protected`인 이유:**
- `public`이면: 개발자가 `new Product()` 로 필드가 전부 비어있는 의미 없는 객체를 만들 수 있어 객체 일관성이 깨진다.
- `private`이면: Hibernate가 지연 로딩(Lazy Loading) 시 생성하는 **프록시 객체**(엔티티를 상속받은 자식 클래스)가 부모 생성자에 접근하지 못해 프록시 생성이 실패한다.
- `protected`는 JPA(프록시 포함)는 접근 가능하게 열어두면서, 외부 개발자의 직접 호출은 막는다.

```java
@NoArgsConstructor(access = AccessLevel.PROTECTED)
```

</details>

---

**Q12. 1차 캐시와 2차 캐시의 차이를 비교하라.**

<details>
<summary>답 보기</summary>

| 구분 | 1차 캐시 | 2차 캐시 |
|---|---|---|
| 관리 주체 | EntityManager (영속성 컨텍스트) | EntityManagerFactory (애플리케이션 전체) |
| 생명주기 | 트랜잭션 단위 | 애플리케이션 전체 (서버 종료까지) |
| 공유 범위 | 같은 트랜잭션 내에서만 | 모든 트랜잭션, 모든 사용자가 공유 |
| 주 목적 | 같은 트랜잭션 내 중복 조회 감소 + 동일성 보장 | 전체 시스템 DB 부하 감소 |
| 주의사항 | - | 자주 변경되는 데이터에 쓰면 stale data(오래된 데이터) 문제 발생 |
| 사용 예시 | 자동 적용 | Ehcache, Redis 연동 필요 |

</details>

---

**Q13. `IDENTITY` 전략에서 쓰기 지연이 적용되지 않는 이유를 "번호표 비유"로 설명하라.**

<details>
<summary>답 보기</summary>

**SEQUENCE 전략 (쓰기 지연 가능):**  
입구에 번호표 뽑기 기계가 있어서 창구(DB)에 가지 않고도 미리 번호(PK)를 받아올 수 있다. 번호를 알고 있으니 실제 업무(INSERT)는 나중에 몰아서 해도 된다.

**IDENTITY 전략 (쓰기 지연 불가):**  
번호표 기계가 없어서 창구(DB)에서 업무(INSERT)를 끝내야만 번호(PK)를 알 수 있다. 번호를 모르는 상태에서는 영속성 컨텍스트가 엔티티를 식별(1차 캐시 등록)할 수 없으므로, `persist()` 즉시 INSERT를 실행해야 한다.

</details>

---

## 심화

---

**Q14. 변경 감지(Dirty Checking)란 무엇이며, 어떤 원리로 동작하는가?**

<details>
<summary>답 보기</summary>

영속성 컨텍스트는 엔티티를 `find()`로 조회한 시점에 **스냅샷(snapshot)** 을 저장해둔다.  
트랜잭션 커밋 시점에 현재 엔티티 상태와 스냅샷을 비교하여, **변경된 필드가 있으면 자동으로 UPDATE SQL을 생성해 실행**한다.

```java
@Transactional
public void updateTitle(Long id, String newTitle) {
    Book book = em.find(Book.class, id); // 스냅샷 저장
    book.changeTitle(newTitle);          // 상태만 변경
    // em.update() 호출 없음!
} // 커밋 시 스냅샷 비교 → UPDATE 자동 발생
```

**이 메커니즘의 특징:**
- 개발자는 "무엇을 바꿀지"만 표현 (선언적 방식)
- "어떻게 SQL을 만들지"는 JPA가 처리
- `find()`로 가져온 엔티티는 JPA가 계속 감시하는 "관리 대상"

</details>

---

**Q15. Hibernate가 기본적으로 변경된 필드만이 아닌 모든 컬럼을 UPDATE 문에 포함시키는 이유는? 변경된 필드만 UPDATE하려면 어떻게 해야 하는가?**

<details>
<summary>답 보기</summary>

**이유 — SQL 캐싱(재사용) 최적화:**  
Hibernate는 엔티티의 모든 필드를 포함한 UPDATE SQL을 미리 만들어서 캐싱한다.  
항상 동일한 형태의 SQL이면 DB가 실행 계획을 캐싱해 재사용할 수 있다.  
반대로 변경된 필드에 따라 동적으로 SQL을 만들면, 필드 N개일 때 최대 2^N개의 서로 다른 SQL이 생겨 DB가 매번 새 실행 계획을 짜야 해서 성능이 나빠진다.

**변경된 필드만 UPDATE하려면 — `@DynamicUpdate`:**

```java
@Entity
@DynamicUpdate
public class Book { ... }
```

단, `@DynamicUpdate`는 SQL 캐싱 이점이 사라지므로 **컬럼 수가 매우 많은(30개+) 엔티티**에서만 효과가 있다. 일반적인 엔티티에서는 기본 설정이 권장된다.

</details>

---

**Q16. `merge()`는 어떤 상황에서 필요하고, 내부적으로 어떻게 동작하는가? `merge()`의 위험성은?**

<details>
<summary>답 보기</summary>

**필요한 상황:**  
트랜잭션 밖으로 나온 엔티티는 **준영속(Detached)** 상태가 되어, 필드 값을 바꿔도 변경 감지가 동작하지 않는다. 이 준영속 엔티티의 변경을 DB에 반영하고 싶을 때 `merge()`를 사용한다.

**동작 원리:**
1. 넘겨준 객체의 PK를 확인
2. 1차 캐시에 해당 PK가 있으면 꺼내오고, 없으면 DB에서 조회해 새 영속 엔티티 생성
3. 그 영속 엔티티의 **모든 필드값을 넘겨준 객체 값으로 통째로 덮어씀**
4. 새로운 영속 엔티티를 반환

```java
Product managed = em.merge(detached);
System.out.println(detached == managed); // false! 다른 객체임
```

**위험성 — 전체 덮어쓰기:**  
일부 필드가 null인 객체를 `merge()` 하면, 원래 DB에 있던 값도 null로 덮어씌워진다.

**권장 패턴:**
```java
// merge() 대신 find() + 변경 메서드 사용
Product product = em.find(Product.class, id);
product.changePrice(newPrice); // 필요한 필드만 안전하게 변경
```

</details>

---

**Q17. `find()`가 1차 캐시를 먼저 확인하는 이유를 "성능" 외의 관점에서 설명하라.**

<details>
<summary>답 보기</summary>

**동일성 보장(Identity Guarantee):**  
같은 트랜잭션 안에서 같은 PK로 조회한 엔티티는 **항상 동일한 인스턴스(`==`)** 여야 한다는 JPA의 보장이다.

만약 매번 DB에서 새 객체를 만든다면:
```java
Product p1 = em.find(Product.class, "p001");
p1.setName("A");
Product p2 = em.find(Product.class, "p001"); // 새 객체라면?
p2.setName("B");
// 커밋 시 어떤 값이 반영되어야 하는가? 모순 발생!
```

1차 캐시가 같은 PK로 같은 인스턴스를 반환하기 때문에, 변경 감지(Dirty Checking)가 정확하게 동작하고 데이터 일관성이 유지된다.

```java
Product p1 = em.find(Product.class, "p001");
Product p2 = em.find(Product.class, "p001");
System.out.println(p1 == p2); // true — 같은 객체
```

</details>

---

**Q18. 아래 코드에서 `SEQUENCE` 전략의 `allocationSize`를 50으로 설정하면 어떤 이점이 있는가? 그리고 이 설정이 DB 시퀀스와 어떻게 맞물려 동작하는가?**

```java
@SequenceGenerator(name = "product_seq", sequenceName = "PRODUCT_SEQ", allocationSize = 50)
```

<details>
<summary>답 보기</summary>

**이점:**  
기본값(`allocationSize = 1`)이면 엔티티를 `persist()`할 때마다 DB 시퀀스를 호출해야 한다(DB 왕복 N번).  
`allocationSize = 50`으로 설정하면 시퀀스를 한 번 호출할 때 **50개의 PK를 미리 확보**해 메모리에 가지고 있다가, 메모리에서 1씩 증가시키며 사용한다. DB 시퀀스 호출이 50번에 1번으로 줄어든다.

**동작 방식:**
- DB 시퀀스는 50씩 증가하도록 설정(`INCREMENT BY 50`)
- JPA는 시퀀스를 호출해 현재 값(예: 1)을 받으면 1~50을 메모리에서 사용
- 51번째 엔티티에서 다시 시퀀스를 호출해 51~100을 확보

**주의:** DB 시퀀스의 `INCREMENT BY`도 `allocationSize`에 맞게 동기화해야 한다. 안 맞으면 PK 중복이나 큰 숫자 점프가 발생할 수 있다.

</details>

---

**Q19. Spring IoC 컨테이너의 싱글톤 빈 관리와 JPA 1차 캐시는 어떤 공통 메커니즘을 공유하는가? 가장 핵심적인 차이는 무엇인가?**

<details>
<summary>답 보기</summary>

**공통 메커니즘 — 레지스트리(Registry) 패턴:**  
둘 다 "**키로 조회했을 때 항상 같은 인스턴스를 돌려주는 Map 기반 저장소**"다.

```
Spring: Map<String, Object> → { "productService" → ProductService 인스턴스 }
JPA:    Map<Object, Object> → { (Product.class, "p001") → Product 인스턴스 }
```

같은 키로 요청하면 새로 만들지 않고 기존 객체를 반환한다.

**가장 핵심적인 차이 — 스코프(생명주기):**

| | Spring 싱글톤 빈 | JPA 1차 캐시 |
|---|---|---|
| 생명주기 | 애플리케이션 전체 (서버 종료까지) | 트랜잭션 1개 동안만 |
| 공유 범위 | 모든 사용자, 모든 요청 | 같은 트랜잭션 내에서만 |

Spring은 "전역적, 영구적" 레지스트리이고, JPA 1차 캐시는 "트랜잭션 단위의 임시" 레지스트리다.

</details>

---

**Q20. 아래 코드가 실행될 때 `findBook1 == findBook2`가 `true`인 이유를 설명하고, 만약 `@GeneratedValue(strategy = GenerationType.SEQUENCE)` 전략이었다면 `em.persist(book)` 호출 직후 INSERT SQL이 나가는가?**

```java
Book book = new Book("제목", "내용");
em.persist(book);
Book findBook1 = em.find(Book.class, book.getId());
Book findBook2 = em.find(Book.class, book.getId());
assertTrue(findBook1 == findBook2);
```

<details>
<summary>답 보기</summary>

**`findBook1 == findBook2`가 `true`인 이유:**  
`@GeneratedValue(strategy = GenerationType.IDENTITY)` 이므로 `em.persist()` 즉시 INSERT가 실행되어 `book.getId()`에 DB가 발급한 PK가 들어간다.  
이후 `em.find()`를 두 번 호출하면 두 번 모두 1차 캐시에서 이미 등록된 같은 객체를 반환한다. DB에 SELECT 쿼리는 나가지 않는다. 1차 캐시는 같은 키(PK)에 대해 항상 동일한 인스턴스를 보장하므로 `==` 비교가 `true`다.

**SEQUENCE 전략이었다면:**  
나가지 않는다. SEQUENCE 전략은 `persist()` 전에 DB 시퀀스 호출(`nextval`)로 PK를 미리 받아온 후, 영속성 컨텍스트에만 등록해두고 INSERT는 커밋 시점까지 **쓰기 지연(Write-Behind)** 한다.  
즉, `em.persist()` 직후에는 INSERT 없이 SELECT(`nextval`)만 발생한다.

</details>

---

## 보너스 — 면접 단골 질문

---

**Q21. JPA의 변경 감지(Dirty Checking)와 MyBatis의 UPDATE 방식의 근본적인 차이를 "선언적 vs 명령형" 관점으로 설명하라.**

<details>
<summary>답 보기</summary>

| 구분 | MyBatis (명령형) | JPA (선언적) |
|---|---|---|
| 개발자가 하는 일 | UPDATE 쿼리를 직접 호출 | 객체의 상태만 변경 |
| SQL 생성 시점 | 개발자가 호출하는 즉시 | 커밋 시점에 JPA가 자동 판단 |
| 사고 방식 | "어떻게 DB를 바꿀 것인가" (SQL 중심) | "객체를 어떤 상태로 만들 것인가" (객체 중심) |

**핵심**: JPA에서는 `find()`로 가져온 엔티티가 "JPA가 계속 감시하는 관리 대상"이다. 따라서 객체의 상태를 바꾸는 것이 곧 "DB를 바꾸겠다는 선언"이 되어, 별도의 UPDATE 호출 없이도 커밋 시점에 변경이 반영된다.

이는 Terraform/Ansible의 "원하는 상태(desired state)만 기술하면, 차이(diff)는 시스템이 처리한다"는 선언적 인프라 관리와 동일한 철학이다.

</details>

---

**Q22. 준영속(Detached) 상태에서 필드 값을 변경하면 DB에 반영되는가? 반영하려면 어떻게 해야 하는가?**

<details>
<summary>답 보기</summary>

**반영되지 않는다.** 준영속 상태는 영속성 컨텍스트가 더 이상 관리하지 않으므로, 변경 감지(Dirty Checking) 대상이 아니다. 값은 메모리에서 바뀌지만 커밋해도 UPDATE가 발생하지 않는다.

**반영 방법 1 — `merge()`:**
```java
Product managed = em.merge(detached); // 값 통째로 복사 후 영속 객체 반환
```
단, 모든 필드를 덮어쓰므로 null 필드가 있으면 기존 DB 값이 날아가는 위험이 있다.

**반영 방법 2 — 권장 패턴 (`find()` + 변경 메서드):**
```java
@Transactional
public void update(Long id, int newPrice) {
    Product product = em.find(Product.class, id); // 영속 상태로 조회
    product.changePrice(newPrice);               // 일부 필드만 안전하게 변경
} // 커밋 시 Dirty Checking → UPDATE
```

</details>

