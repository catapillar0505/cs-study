# JS 개념 정리

---

## 1. 호이스팅 (Hoisting)

JS 엔진이 코드 실행 전에 **변수/함수 선언을 스코프 최상단으로 끌어올리는 동작**.
실제로 코드가 이동하는 게 아니라, 컴파일 단계에서 선언을 먼저 메모리에 등록하는 것.

| 종류 | 호이스팅 | 초기값 |
|---|---|---|
| `var` | O | `undefined` |
| `let` / `const` | O (TDZ) | 접근 불가 |
| 함수 선언식 | O | 함수 전체 |
| 함수 표현식 | 변수만 | `undefined` |

**Java 비교:**
| JS | Java 느낌 |
|---|---|
| 함수 선언식 호이스팅 | 클래스 안에서 메서드 순서 무관 |
| `let`/`const` | 일반 지역변수 (선언 전 접근 불가) |
| `var` | Java에 없는 개념 |

---

## 2. JS 객체 vs 클래스 인스턴스

**다르지만, JS 객체가 Java 클래스 인스턴스를 포함하는 더 넓은 개념.**

```js
// 객체 리터럴 (클래스 없이 바로 생성)
const person = { name: "김지나", age: 25 };

// 클래스 인스턴스 (이것도 결국 JS 객체)
class Person {
    constructor(name) { this.name = name; }
}
const p = new Person("김지나");
typeof p // "object"
```

| | Java 인스턴스 | JS 객체 |
|---|---|---|
| 클래스 필요? | 필수 | 선택 |
| 설계도 없이 생성 | 불가 | 가능 |

---

## 3. 객체 리터럴

`{ }` 중괄호로 **그 자리에서 바로 객체를 찍어내는 것.**

```js
const person = {
    name: "김지나",  // 프로퍼티
    age: 25          // 프로퍼티
};
```

---

## 4. 동적 타입 vs 약한 타입

| | 의미 |
|---|---|
| **동적 타입** | 타입 선언 없이 변수 사용, 실행할 때 타입 결정 |
| **약한 타입** | 다른 타입끼리 연산 시 자동 형변환 |

```js
// 동적 타입
let x = 5;
x = "안녕"; // 가능

// 약한 타입
"5" + 1   // "51"
"5" - 1   // 4
"5" == 5  // true
"5" === 5 // false (타입까지 비교)
```

---

## 5. 프로퍼티와 메서드

- **프로퍼티**: `this.xxx`로 인스턴스에 붙는 값
- **메서드**: 객체/클래스에 정의된 함수

```js
class Person {
    constructor(name) {
        this.name = name; // 프로퍼티
    }
    hello() {             // 메서드
        console.log("안녕");
    }
}
```

**한 줄 요약: 객체 안에 있으면 메서드, 밖에 있으면 함수.**

---

## 6. DOM (Document Object Model)

브라우저가 HTML을 읽고 메모리에 만드는 **트리 구조 전체.**

```
window
 └── document       ← DOM 시작점(루트)
      └── body
           └── div
                └── button
```

- HTML = 종이 설계도
- DOM = 실제로 지어진 집

### DOM 조작

```js
// 요소 선택
document.querySelector("#title");       // id
document.querySelectorAll(".item");     // class (여러 개)

// 내용 변경
title.textContent = "바뀐 제목";
title.style.color = "red";
title.classList.add("active");
title.classList.toggle("active");

// 생성/삭제
const div = document.createElement("div");
document.body.appendChild(div);
div.remove();
```

---

## 7. 이벤트 (Event)

사용자가 뭔가 했을 때 JS한테 알려주는 신호.

```js
btn.addEventListener("이벤트 종류", 실행할 함수, options);
```

### 자주 쓰는 이벤트

| 이벤트 | 언제? |
|---|---|
| `click` | 클릭했을 때 |
| `mouseover` | 마우스 올렸을 때 |
| `keydown` | 키보드 눌렀을 때 |
| `input` | 타이핑할 때 |
| `submit` | 폼 제출할 때 |

### 세 번째 인자 options

```js
element.addEventListener("click", handler, {
    once: true,    // 딱 한 번만 실행
    passive: true, // 스크롤 성능 향상
    capture: true  // 캡처링 단계에서 실행
});
```

---

## 8. e.target vs e.currentTarget

```js
document.querySelector("#parent").addEventListener("click", function(e) {
    e.target;        // 실제로 클릭된 요소 (출발지)
    e.currentTarget; // addEventListener가 달린 요소
});
```

| | 의미 |
|---|---|
| `e.target` | 실제로 클릭된 요소 |
| `e.currentTarget` | 이벤트 리스너가 달린 요소 |

---

## 9. 이벤트 전파 (캡처링 & 버블링)

클릭 시 항상 3단계가 세트로 발생:

```
캡처링: window → document → body → div → button  (위 → 아래)
타깃:   button (이벤트 발생)
버블링: button → div → body → document → window  (아래 → 위)
```

- **캡처링** (`true`): 내려가면서 부모가 먼저 낚아챔
- **버블링** (`false`, 기본값): 자식까지 내려보낸 후 올라오며 부모에게 알림

```js
element.addEventListener("click", handler, false); // 버블링 (기본)
element.addEventListener("click", handler, true);  // 캡처링
```

버블링 때 전파되는 건 **이벤트 재실행이 아니라, 발생 정보(e 객체)가 위로 전달**되는 것.

---

## 10. 이벤트 위임 (Event Delegation)

버블링 원리를 이용해 **자식 이벤트를 부모가 대신 처리**하는 패턴.

```js
// ❌ 자식마다 리스너 다는 것
btn1.addEventListener("click", handler);
btn2.addEventListener("click", handler);

// ✅ 이벤트 위임 (부모 하나에만)
document.querySelector("#parent").addEventListener("click", function(e) {
    if (e.target.tagName === "BUTTON") {
        handler();
    }
});
```

**장점**: 리스너 1개로 메모리 절약, 동적으로 추가되는 자식도 처리 가능.

### closest() 활용

```js
// 클릭된 게 <i>이든 <span>이든 가장 가까운 .menu-item 찾기
const btn = e.target.closest('.menu-item');
if (!btn) return;

const menuName = btn.dataset.menu; // data-menu 속성값 꺼내기
```

---

## 11. this 키워드

함수를 **어떻게 호출하느냐**에 따라 가리키는 대상이 동적으로 변함.

| 호출 방법 | this |
|---|---|
| `person.hello()` | person 객체 |
| `hello()` | window (전역) |
| `new Hello()` | 새로 생성된 인스턴스 |
| `addEventListener` 콜백 | 이벤트 달린 요소 |
| 화살표 함수 `() =>` | 상위 스코프의 this 그대로 |

**Java 비교**: Java `this`는 태어날 때 정해지고, JS `this`는 불릴 때 정해짐.

---

## 12. 화살표 함수 vs 콜백 함수

| | 화살표 함수 | 콜백 함수 |
|---|---|---|
| 뭐야? | 함수 선언 **문법** | 함수 사용 **방식** |
| 특징 | this 고정, 문법 간결 | 다른 함수 인자로 전달 |

```js
// 화살표 함수
const add = (a, b) => a + b;

// 콜백 함수 (화살표 함수가 콜백으로 쓰임)
arr.forEach(x => console.log(x));
btn.addEventListener("click", () => console.log("클릭!"));
```

**this 차이:**
```js
setTimeout(function() {
    console.log(this.name); // undefined (this가 바뀜)
}, 1000);

setTimeout(() => {
    console.log(this.name); // 상위 스코프 this 유지
}, 1000);
```

---

## 13. arguments vs rest 파라미터

| | `arguments` | rest 파라미터 |
|---|---|---|
| 자동 생성 | O | X (직접 써야 함) |
| 진짜 배열? | X (유사 배열) | O |
| 배열 메서드 | 못 씀 | 됨 |
| 화살표 함수 | 사용 불가 | 가능 |
| 일부만 받기 | 불가 | 가능 |

```js
// arguments
function hello() {
    console.log(arguments); // { 0: 1, 1: 2, 2: 3 }
}

// rest 파라미터
function hello(a, ...rest) {
    console.log(a);    // 1
    console.log(rest); // [2, 3, 4]
}
```

---

## 14. 프로토타입 ([[Prototype]])

JS의 모든 객체는 **[[Prototype]]을 자동으로 가짐.** Java의 모든 클래스가 Object를 상속하는 것과 같음.

```
배열 data
 ├── 0: { id, url }            ← 객체
 │    └── [[Prototype]]: Object ← 객체가 Object 상속
 └── [[Prototype]]: Array(0)   ← 배열이 Array 상속
      └── [[Prototype]]: Object ← Array도 Object 상속
```

---

## 15. 비동기 (Asynchronous)

### 상위/하위 개념 구조

```
비동기 (Asynchronous)
 ├── AJAX (비동기 통신 기술)
 │    ├── XMLHttpRequest (옛날)
 │    ├── fetch API      (요즘, 내장)
 │    └── axios          (요즘, 라이브러리)
 │
 └── Promise (비동기 처리 패턴)
      └── async/await (Promise 문법적 설탕)
```

- **AJAX** = 서버랑 통신하는 방법
- **Promise** = 그 결과를 처리하는 방법

### 발전 순서

```js
// 1. XMLHttpRequest (옛날)
xhr.onreadystatechange = function() {
    if (xhr.readyState === 4 && xhr.status === 200) {
        console.log(xhr.responseText);
    }
};

// 2. fetch + then (2015)
fetch("/api/user")
    .then(res => res.json())
    .then(data => console.log(data))
    .catch(err => console.log(err));

// 3. fetch + async/await (2017)
async function getUser() {
    const res = await fetch("/api/user");
    const data = await res.json();
    console.log(data);
}
```

### fetch vs axios

| | fetch | axios |
|---|---|---|
| 종류 | 브라우저 내장 | 외부 라이브러리 |
| 설치 | 불필요 | `npm install axios` |
| JSON 자동변환 | X | O |
| 에러 처리 | 불편 (직접 체크) | 편함 (자동) |
| 인터셉터 | X | O |

```js
// fetch
const res = await fetch("/api/user");
const data = await res.json(); // JSON 변환 직접

// axios
const { data } = await axios.get("/api/user"); // 자동 변환
```

**실무**: React/Vue → axios, Next.js → fetch

### await가 비동기인 이유

```js
async function getUserName() {
    const data = await fetch("/api/user"); // 이 함수만 대기
}

// 함수 밖은 계속 실행됨 ← 블로킹이 아닌 이유
button.click(); // 됨
scroll();       // 됨
```

동기(블로킹)는 JS 전체가 멈추지만, `await`는 **해당 함수만 멈추고 나머지는 계속 실행.**